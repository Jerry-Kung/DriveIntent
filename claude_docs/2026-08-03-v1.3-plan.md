# DriveIntent V1.3 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现 V1.3——评论/账号两级"是否车主"（`is_car_owner`）与"购车意向"（`has_purchase_intent`）独立标签、初筛通过规则重构（新增 `no_purchase_intent`、取消 `model_mismatch`/`existing_owner`/`ordered_owner`）、视频语境降级模块下线、非本人意向识别 bugfix。

**Architecture:** LLM 负责判断（输出标签与信号），代码层负责规则（`resolve_filter_type` 确定性合成 filter_type/passed）。Agent1 初筛 LLM 输出 3 个新布尔（含内部信号 `positive_attitude`），Agent2 输出 2 个新布尔；8000 流水线与 8100 API 共享同一通过判定函数。设计文档：`claude_docs/2026-08-03-v1.3-design.md`（必读，尤其第 3 节规则表与第 7 节提示词指引）。

**Tech Stack:** Python 3.14 / FastAPI / SQLAlchemy / Pydantic v2 / pytest（mock LLM 模式，无真实 LLM 调用）。

## Global Constraints

- 全部代码注释、文档、提交信息用简体中文；沿用仓库现有代码风格（4 空格缩进、行宽约 79、中文注释）。
- 每个任务结束时全量测试必须通过：`python -m pytest tests/ -q`（项目根目录 `D:\KLH\Projects\dfms\DriveIntent` 下执行）。
- 提交信息格式沿用仓库惯例（`feat:` / `test:` / `docs:` / `refactor:` 前缀），结尾加 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`。
- 对外契约字段名固定：`is_car_owner`、`has_purchase_intent`、`no_purchase_intent`（枚举值）；内部信号字段名固定：`positive_attitude`（不得对外透出）。
- `filter_reason` 字段保留在 `ScreeningResult` 中但 V1.3 起恒为 `null`。
- YAGNI：`Lead` 表不加列；不新增意向主体结构化字段。

---

### Task 1: 下线视频语境降级模块

**Files:**
- Delete: `app/matching/downgrade.py`
- Delete: `tests/test_matching_downgrade.py`
- Modify: `app/api/agent1.py`
- Modify: `app/api/mapping.py:38-51`（`map_screening_item` 去掉 `mismatch_reason` 参数）
- Modify: `app/config.py:37-39`（删除 `intent_downgrade_enabled`）
- Modify: `tests/test_agent1.py`（删除降级相关测试）
- Modify: `tests/test_api_mapping.py:134-152`（删除 model_mismatch 测试）
- Modify: `tests/test_matching_loader.py:75-79`
- Modify: `tests/test_v11_integration.py`（cm_1 断言改为 genuine_user）

**Interfaces:**
- Consumes: 现有 `map_screening_item(item, processed_at, *, mismatch_reason)`。
- Produces: `map_screening_item(item: CommentScreeningItem, processed_at: str) -> ScreeningResult`（无 keyword 参数）。Task 2 将在此基础上继续改造。`app/matching/loader.py` / `models.py` 保留不动（Agent2 依赖）。

- [ ] **Step 1: 更新 `tests/test_api_mapping.py`——删除 model_mismatch 测试**

删除 `test_map_screening_model_mismatch_marks_and_passes`（134-143 行）与 `test_map_screening_mismatch_not_applied_to_filtered`（145-152 行）两个函数。

- [ ] **Step 2: 更新 `tests/test_agent1.py`——删除降级测试与配置装置**

删除以下内容：`_OUR_MODELS`、`_CTX_MISMATCH`、`_CTX_OURS`、`_SCREEN_HIGH` 常量（48-73 行）、`_setup_config` 函数（76-82 行）、`test_mismatch_marked_for_mismatched_video`、`test_no_mismatch_for_our_model_video`、`test_no_mismatch_when_disabled`、`test_no_mismatch_when_config_missing` 四个测试（85-124 行）。

保留并修改剩余两个测试（`test_owner_comment_filtered`、`test_failed_item_keys`），去掉 `_setup_config` 依赖，语境改用内联 JSON：

```python
_CTX = json.dumps({"brand": "测试", "content_type": "试驾"})


@pytest.mark.asyncio
async def test_owner_comment_filtered():
    screening = json.dumps({"items": [
        {"comment_id": "cm_1", "is_meaningful": True,
         "owner_status": "ordered_owner", "intent_strength": "low",
         "reason": "大定已下等提车"},
        {"comment_id": "cm_2", "is_meaningful": False, "reason": "刷屏"}]})
    out = await run_comment_screening(_executor(_CTX, screening), _req())
    r = out["results"][0]
    assert r["passed"] is False
    assert r["filter_type"] == "ordered_owner"
    assert r["filter_reason"] is None


@pytest.mark.asyncio
async def test_failed_item_keys():
    # 只给语境响应，筛选批次 Mock 无响应 → 整批失败
    out = await run_comment_screening(_executor(_CTX), _req())
    r = out["results"][0]
    assert r["error"]
    assert r["filter_type"] is None
    assert r["filter_reason"] is None
    assert "intent_strength" not in r
```

（`test_owner_comment_filtered` 在 Task 2 会再改为 V1.3 语义，此处仅去除降级依赖保持绿灯。）

- [ ] **Step 3: 更新 `tests/test_matching_loader.py`——删除降级开关断言**

`test_settings_new_fields_defaults`（75-79 行）删除 `assert s.intent_downgrade_enabled is True` 一行，保留 `our_models_config_path` 断言。

- [ ] **Step 4: 更新 `tests/test_v11_integration.py`——cm_1 不再标记 model_mismatch**

fixture `env` 中删除 `monkeypatch.setattr(settings, "intent_downgrade_enabled", True)`（35 行）。cm_1 的断言块（94-100 行）改为：

```python
    # cm_1：真实用户询价 → 通过初筛（V1.3 起初筛不再考虑车型匹配）
    assert r1["passed"] is True
    assert r1["filter_type"] == "genuine_user"
    assert r1["filter_reason"] is None
    assert "intent_strength" not in r1
```

- [ ] **Step 5: 运行测试确认失败（当前实现仍会输出 model_mismatch）**

Run: `python -m pytest tests/test_v11_integration.py tests/test_agent1.py -q`
Expected: FAIL——cm_1 仍被标为 `model_mismatch`（实现未改）。

- [ ] **Step 6: 修改 `app/api/mapping.py`——`map_screening_item` 去掉降级参数**

替换 38-51 行为：

```python
def map_screening_item(item: CommentScreeningItem,
                       processed_at: str) -> ScreeningResult:
    filter_type = resolve_filter_type(item)
    passed = filter_type == "genuine_user"
    analysis = item.reason or ("通过初筛。" if passed else "未通过初筛。")
    return ScreeningResult(comment_id=item.comment_id, passed=passed,
                           filter_type=filter_type,
                           analysis=analysis, processed_at=processed_at)
```

（`filter_reason` 不再赋值，取 schema 默认 None。注意此时 `resolve_filter_type` 仍是旧版，Task 2 再重写。旧版中 `existing_owner`/`ordered_owner` 仍可能返回，`passed = filter_type == "genuine_user"` 与旧行为一致——旧版里这两类本就 passed=False。）

- [ ] **Step 7: 修改 `app/api/agent1.py`——移除降级调用链**

- 删除 import（6-7 行）：`from app.matching.downgrade import ...` 与 `from app.matching.loader import load_our_models`；`from app.config import settings` 保留（`comment_batch_size` 仍用）。
- 删除 `run_comment_screening` 中：`our_models = ...` 与 `decisions: dict[...] = {}`（57-60 行）、语境评估后的 `decisions[title] = evaluate_video_context(...)`（68-70 行）。
- 回填循环（89-95 行）改为：

```python
        item = items.get(c.comment_id)
        if item is not None:
            results.append(map_screening_item(item, ts).model_dump())
```

- [ ] **Step 8: 修改 `app/config.py`——删除降级开关**

删除 39 行 `intent_downgrade_enabled: bool = True`；37 行注释改为 `# 我方在售车型配置（Agent2 评级用）`。

- [ ] **Step 9: 删除降级模块与测试文件**

```powershell
git rm app/matching/downgrade.py tests/test_matching_downgrade.py
```

- [ ] **Step 10: 全量测试**

Run: `python -m pytest tests/ -q`
Expected: 全部 PASS。

- [ ] **Step 11: 提交**

```powershell
git add -A; git commit -m @'
refactor(v1.3): 下线视频语境车型匹配降级模块

初筛阶段不再考虑车型匹配（model_mismatch 取消），删除 downgrade.py
及调用链、intent_downgrade_enabled 开关。matching/loader 保留供
Agent2 车型匹配度评级使用。

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
'@
```

---

### Task 2: Agent1 两标签 schema 与通过规则重写

**Files:**
- Modify: `app/schemas/skills.py:23-42`（`CommentScreeningItem`）
- Modify: `app/api/mapping.py:20-35`（`resolve_filter_type` 重写）、`map_screening_item`（透出新字段）
- Modify: `app/api/schemas.py:58-67`（`ScreeningResult` 新增两字段）
- Modify: `app/api/agent1.py`（失败条目 dict 补新字段 null）
- Modify: `tests/test_api_skill_schema.py`、`tests/test_api_mapping.py`、`tests/test_agent1.py`
- Rename+Rewrite: `tests/test_v11_integration.py` → `tests/test_v13_integration.py`

**Interfaces:**
- Consumes: Task 1 的 `map_screening_item(item, processed_at)`。
- Produces:
  - `CommentScreeningItem` 新字段：`is_car_owner: bool = False`、`has_purchase_intent: bool = False`、`positive_attitude: bool = False`；**移除** `owner_status`。
  - `resolve_filter_type(item: CommentScreeningItem) -> str`——返回值域：`genuine_user | no_purchase_intent | bot_spam | marketing_account | noise | off_topic`。
  - `ScreeningResult` 新字段：`is_car_owner: bool | None = None`、`has_purchase_intent: bool | None = None`。
  - Task 4 依赖 `resolve_filter_type` 的新规则语义。

- [ ] **Step 1: 写 schema 失败测试（`tests/test_api_skill_schema.py`）**

替换 19-39 行的三个 v11 测试为：

```python
def test_screening_item_v13_new_fields_defaults():
    from app.schemas.skills import CommentScreeningItem
    item = CommentScreeningItem(comment_id="c1")
    assert item.is_car_owner is False
    assert item.has_purchase_intent is False
    assert item.positive_attitude is False
    assert item.comment_actor == "genuine_user"


def test_screening_item_v13_owner_status_removed():
    from app.schemas.skills import CommentScreeningItem
    # 旧字段作为多余输入被忽略（历史 LLM 输出兼容），不再是模型字段
    item = CommentScreeningItem(comment_id="c1", owner_status="ordered_owner")
    assert "owner_status" not in item.model_dump()


def test_screening_item_v13_label_values():
    from app.schemas.skills import CommentScreeningItem
    item = CommentScreeningItem(comment_id="c1", is_car_owner=True,
                                has_purchase_intent=True)
    assert item.is_car_owner is True
    assert item.has_purchase_intent is True
```

- [ ] **Step 2: 写通过规则失败测试（`tests/test_api_mapping.py`）**

删除旧的 owner 相关测试：`test_resolve_filter_type_priority_actor_over_owner`（79-85 行）、`test_resolve_filter_type_owner_values`（88-95 行）、`test_map_screening_owner_filtered_no_reason_text`（110-118 行）、`test_map_screening_ordered_owner`（121-125 行）。同时更新 `test_map_screening_passed`（9-15 行，构造参数加 `has_purchase_intent=True`）。新增规则表全分支测试：

```python
def _item(**kw):
    from app.schemas.skills import CommentScreeningItem
    base = dict(comment_id="c", is_meaningful=True,
                is_suspected_marketing=False)
    base.update(kw)
    return CommentScreeningItem(**base)


def test_v13_rule1_actor_abnormal_wins():
    # 优先级1：actor 异常类优先于意向标签
    from app.api.mapping import resolve_filter_type
    item = _item(comment_actor="marketing_account",
                 has_purchase_intent=True, is_car_owner=True)
    assert resolve_filter_type(item) == "marketing_account"


def test_v13_rule2_intent_always_passes():
    # 优先级2：有购车意向必过筛（车主与否均然）
    from app.api.mapping import resolve_filter_type, map_screening_item
    for owner in (True, False):
        item = _item(has_purchase_intent=True, is_car_owner=owner)
        assert resolve_filter_type(item) == "genuine_user"
        assert map_screening_item(item, "t").passed is True


def test_v13_rule3_owner_without_intent_filtered():
    # 优先级3：无意向车主（纯讨论/吐槽）不过筛
    from app.api.mapping import map_screening_item
    item = _item(is_car_owner=True, has_purchase_intent=False,
                 positive_attitude=True, reason="我这台油耗8个")
    r = map_screening_item(item, "t")
    assert r.passed is False
    assert r.filter_type == "no_purchase_intent"
    assert r.filter_reason is None


def test_v13_rule4_non_owner_positive_passes():
    # 优先级4：无意向非车主有积极信号 → 过筛（B级弱线索）
    from app.api.mapping import map_screening_item
    item = _item(is_car_owner=False, has_purchase_intent=False,
                 positive_attitude=True, reason="内饰真好看")
    r = map_screening_item(item, "t")
    assert r.passed is True
    assert r.filter_type == "genuine_user"
    assert r.has_purchase_intent is False


def test_v13_rule5_non_owner_no_signal_filtered():
    # 优先级5：无意向非车主无积极信号 → 不过筛
    from app.api.mapping import map_screening_item
    item = _item(is_car_owner=False, has_purchase_intent=False,
                 positive_attitude=False)
    r = map_screening_item(item, "t")
    assert r.passed is False
    assert r.filter_type == "no_purchase_intent"


def test_v13_result_carries_labels():
    from app.api.mapping import map_screening_item
    item = _item(is_car_owner=True, has_purchase_intent=True)
    d = map_screening_item(item, "t").model_dump()
    assert d["is_car_owner"] is True
    assert d["has_purchase_intent"] is True
    assert "positive_attitude" not in d       # 内部信号不对外
    assert "owner_status" not in d


def test_v13_legacy_fallback_kept():
    # LLM 未输出 comment_actor 时回退 V1.0 判定（旧字段兜底）
    from app.api.mapping import resolve_filter_type
    marketing = _item(is_suspected_marketing=True, has_purchase_intent=True)
    noise = _item(is_meaningful=False)
    assert resolve_filter_type(marketing) == "marketing_account"
    assert resolve_filter_type(noise) == "noise"
```

- [ ] **Step 3: 运行确认失败**

Run: `python -m pytest tests/test_api_skill_schema.py tests/test_api_mapping.py -q`
Expected: FAIL（新字段不存在 / no_purchase_intent 未实现）。

- [ ] **Step 4: 修改 `app/schemas/skills.py`——`CommentScreeningItem`**

替换 34-37 行（owner_status 与 comment_actor 注释块）为：

```python
    # V1.1：评论主体类型（异常类优先过滤）
    comment_actor: Literal["genuine_user", "bot_spam", "marketing_account",
                           "noise", "off_topic"] = "genuine_user"
    # V1.3：独立分析标签（不要求与我方在售车型相关）与内部积极信号。
    # is_car_owner：有明确证据大概率已购车（含已下单/下大定）；
    # has_purchase_intent：表达了任何购车相关倾向（本人意向）；
    # positive_attitude：非车主无意向但表达兴趣/赞美，仅供代码层
    # 合成 filter_type 使用，不进对外契约。
    is_car_owner: bool = False
    has_purchase_intent: bool = False
    positive_attitude: bool = False
```

（即：删除 `owner_status` 字段，保留 `comment_actor`，追加三个布尔。）

- [ ] **Step 5: 修改 `app/api/schemas.py`——`ScreeningResult`**

在 `filter_type` 之后插入：

```python
    # V1.3：独立分析标签；该条处理失败时为 null
    is_car_owner: bool | None = None
    has_purchase_intent: bool | None = None
```

`filter_reason` 注释（61-63 行）改为：`# V1.3：恒为 null（原 model_mismatch 场景已取消），保留字段避免契约结构变动`。

- [ ] **Step 6: 修改 `app/api/mapping.py`——重写 `resolve_filter_type` 与 `map_screening_item`**

```python
def resolve_filter_type(item: CommentScreeningItem) -> str:
    """合成 filter_type。优先级：actor 异常类 > 旧字段兜底 > 意向/车主规则。

    V1.3：passed 与 filter_type 严格一一对应（genuine_user 通过，其余不过）。
    有购车意向必过筛；无意向车主不过筛；无意向非车主看积极信号。
    """
    if item.comment_actor != "genuine_user":
        return item.comment_actor
    # 兼容 LLM 未输出 comment_actor 的情况，回退 V1.0 判定
    if item.is_suspected_marketing:
        return "marketing_account"
    if not item.is_meaningful:
        return "noise"
    if item.has_purchase_intent:
        return "genuine_user"
    if item.is_car_owner:
        return "no_purchase_intent"
    if item.positive_attitude:
        return "genuine_user"
    return "no_purchase_intent"


def map_screening_item(item: CommentScreeningItem,
                       processed_at: str) -> ScreeningResult:
    filter_type = resolve_filter_type(item)
    passed = filter_type == "genuine_user"
    analysis = item.reason or ("通过初筛。" if passed else "未通过初筛。")
    return ScreeningResult(comment_id=item.comment_id, passed=passed,
                           filter_type=filter_type, analysis=analysis,
                           is_car_owner=item.is_car_owner,
                           has_purchase_intent=item.has_purchase_intent,
                           processed_at=processed_at)
```

- [ ] **Step 7: 修改 `app/api/agent1.py`——失败条目补 null 标签**

失败分支 dict 改为：

```python
            err = errors.get(c.comment_id, "筛选失败")
            results.append({"comment_id": c.comment_id, "passed": False,
                            "filter_reason": None, "filter_type": None,
                            "is_car_owner": None, "has_purchase_intent": None,
                            "analysis": "", "processed_at": ts, "error": err})
```

- [ ] **Step 8: 更新 `tests/test_agent1.py`——owner 测试改 V1.3 语义、失败条目断言补充**

`test_owner_comment_filtered` 重写为：

```python
@pytest.mark.asyncio
async def test_owner_without_intent_filtered():
    # V1.3：无购车意向的车主（纯讨论）不过筛，filter_type=no_purchase_intent
    screening = json.dumps({"items": [
        {"comment_id": "cm_1", "is_meaningful": True, "is_car_owner": True,
         "has_purchase_intent": False, "reason": "我这台开了2万公里，油耗8个"},
        {"comment_id": "cm_2", "is_meaningful": False, "reason": "刷屏"}]})
    out = await run_comment_screening(_executor(_CTX, screening), _req())
    r = out["results"][0]
    assert r["passed"] is False
    assert r["filter_type"] == "no_purchase_intent"
    assert r["is_car_owner"] is True
    assert r["has_purchase_intent"] is False


@pytest.mark.asyncio
async def test_owner_with_trade_in_intent_passes():
    # V1.3：车主表达增换购意向 → 必过筛（V1.1 时代恒被过滤，行为变更点）
    screening = json.dumps({"items": [
        {"comment_id": "cm_1", "is_meaningful": True, "is_car_owner": True,
         "has_purchase_intent": True, "intent_strength": "high",
         "reason": "开了5年想置换升级"},
        {"comment_id": "cm_2", "is_meaningful": False, "reason": "刷屏"}]})
    out = await run_comment_screening(_executor(_CTX, screening), _req())
    r = out["results"][0]
    assert r["passed"] is True
    assert r["filter_type"] == "genuine_user"
    assert r["is_car_owner"] is True
    assert r["has_purchase_intent"] is True
```

`test_failed_item_keys` 补两行断言：

```python
    assert r["is_car_owner"] is None
    assert r["has_purchase_intent"] is None
```

- [ ] **Step 9: 重写集成测试 `tests/test_v11_integration.py` → `tests/test_v13_integration.py`**

`git mv tests/test_v11_integration.py tests/test_v13_integration.py`，文件整体重写（fixture 保持结构，`_OUR_MODELS` 写盘可删——初筛已不读该配置，仅保留 api_keys 与 DB 装置）：

```python
import json

import pytest
from fastapi import FastAPI
from fastapi.testclient import TestClient
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker
from sqlalchemy.pool import StaticPool

from app.api.routes import api_router, get_db
from app.api.worker import ApiJobWorker
from app.db import Base
from app.llm.gateway import LLMGateway
from app.llm.mock import MockProvider
from app.skills.executor import SkillExecutor


@pytest.fixture()
def env(monkeypatch):
    from app.config import settings
    monkeypatch.setattr(settings, "api_keys", "secret")
    engine = create_engine("sqlite://",
                           connect_args={"check_same_thread": False},
                           poolclass=StaticPool)
    import app.models  # noqa
    Base.metadata.create_all(engine)
    Session = sessionmaker(bind=engine, expire_on_commit=False)
    app = FastAPI()
    app.include_router(api_router)
    app.dependency_overrides[get_db] = lambda: Session()
    return app, Session


def _payload():
    def c(cid, content):
        return {"comment_id": cid, "video_title": "微光mini测评",
                "video_author": "@测评君", "video_author_fans": 10000,
                "comment_content": content, "comment_author": "a",
                "comment_author_uid": f"u_{cid}",
                "comment_time": "2026-07-27T10:00:00+08:00",
                "comment_like_count": 1}
    return {"comments": [c("cm_1", "落地多少钱"),
                         c("cm_2", "我这台开了2万公里，油耗8个"),
                         c("cm_3", "开了5年了，想置换升级"),
                         c("cm_4", "内饰真好看"),
                         c("cm_5", "加V了解低息方案")]}


@pytest.mark.asyncio
async def test_v13_screening_end_to_end(env):
    app, Session = env
    client = TestClient(app)
    r = client.post("/api/v1/comment-screening", json=_payload(),
                    headers={"Authorization": "Bearer secret"})
    job_id = r.json()["job_id"]

    provider = MockProvider()
    provider.queue(
        json.dumps({"brand": "微光", "model": "微光mini",
                    "vehicle_category": "微型车"}),
        json.dumps({"items": [
            {"comment_id": "cm_1", "is_meaningful": True,
             "comment_actor": "genuine_user", "is_car_owner": False,
             "has_purchase_intent": True, "intent_strength": "high",
             "reason": "询问落地价"},
            {"comment_id": "cm_2", "is_meaningful": True,
             "comment_actor": "genuine_user", "is_car_owner": True,
             "has_purchase_intent": False, "positive_attitude": False,
             "reason": "车主用车讨论，无购车意向"},
            {"comment_id": "cm_3", "is_meaningful": True,
             "comment_actor": "genuine_user", "is_car_owner": True,
             "has_purchase_intent": True, "intent_strength": "medium",
             "reason": "车主表达置换升级意向"},
            {"comment_id": "cm_4", "is_meaningful": True,
             "comment_actor": "genuine_user", "is_car_owner": False,
             "has_purchase_intent": False, "positive_attitude": True,
             "reason": "非车主表达产品兴趣"},
            {"comment_id": "cm_5", "is_meaningful": True,
             "comment_actor": "marketing_account", "is_car_owner": False,
             "has_purchase_intent": False, "reason": "引流广告"}]}))
    executor = SkillExecutor(LLMGateway(provider))
    worker = ApiJobWorker(lambda: Session(), executor, LLMGateway(provider))
    await worker.run_once()

    body = client.get(f"/api/v1/jobs/{job_id}",
                      headers={"Authorization": "Bearer secret"}).json()
    assert body["status"] == "success"
    r1, r2, r3, r4, r5 = body["result"]["results"]
    # cm_1：非车主有意向 → 过筛
    assert (r1["passed"], r1["filter_type"]) == (True, "genuine_user")
    assert (r1["is_car_owner"], r1["has_purchase_intent"]) == (False, True)
    # cm_2：车主无意向纯讨论 → 不过筛
    assert (r2["passed"], r2["filter_type"]) == (False, "no_purchase_intent")
    assert (r2["is_car_owner"], r2["has_purchase_intent"]) == (True, False)
    # cm_3：车主增换购意向 → 必过筛
    assert (r3["passed"], r3["filter_type"]) == (True, "genuine_user")
    # cm_4：非车主积极信号 → 过筛（B级弱线索）
    assert (r4["passed"], r4["filter_type"]) == (True, "genuine_user")
    assert r4["has_purchase_intent"] is False
    # cm_5：营销号 → 过滤；全部条目 filter_reason 恒 null、无内部信号泄漏
    assert (r5["passed"], r5["filter_type"]) == (False, "marketing_account")
    for item in (r1, r2, r3, r4, r5):
        assert item["filter_reason"] is None
        assert "positive_attitude" not in item
        assert "owner_status" not in item
```

- [ ] **Step 10: 全量测试**

Run: `python -m pytest tests/ -q`
Expected: 全部 PASS（注意 `tests/test_skill_executor.py` 的 v2 提示词测试仍检查 `owner_status` 文本——v2 模板未动，仍通过；Task 3 一并更新）。

- [ ] **Step 11: 提交**

```powershell
git add -A; git commit -m @'
feat(v1.3): Agent1 车主/购车意向独立标签与初筛通过规则重写

- CommentScreeningItem 新增 is_car_owner/has_purchase_intent/positive_attitude，移除 owner_status
- resolve_filter_type 五级规则：有意向必过筛，无意向车主不过筛，非车主看积极信号
- filter_type 新增 no_purchase_intent，取消 existing_owner/ordered_owner
- ScreeningResult 对外透出两标签，失败条目为 null

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
'@
```

---

### Task 3: 初筛提示词 v3 与技能升版

**Files:**
- Create: `app/skills/prompts/comment_lead_screening_v3.txt`
- Modify: `app/skills/configs/comment_lead_screening.yaml`
- Modify: `app/workflow/pipeline.py:19-23`（`SKILL_VERSIONS` 初筛升 1.3）
- Modify: `tests/test_skill_executor.py:107-133`

**Interfaces:**
- Consumes: Task 2 的 `CommentScreeningItem` 新字段（LLM 输出 JSON 键与之对齐）。
- Produces: `comment_lead_screening` 技能 version="1.3"、prompt_version="v3"；`SKILL_VERSIONS[COMMENT_SCREENING_SKILL] == "1.3"`。占位符与 v2 相同：`$video_context_json` / `$comments_json` / `$comment_count`。

- [ ] **Step 1: 写失败测试（`tests/test_skill_executor.py`）**

替换 107-133 行的两个 screening 测试（`test_v11_screening_config_uses_v2`、`test_v11_screening_prompt_renders_with_owner_fields`）为：

```python
def test_v13_screening_config_uses_v3():
    from app.skills.executor import load_skill_config
    config = load_skill_config("comment_lead_screening")
    assert config.prompt_file == "comment_lead_screening_v3.txt"
    assert config.prompt_version == "v3"
    assert config.version == "1.3"


def test_v13_screening_prompt_renders_with_label_fields():
    from app.skills.executor import load_skill_config, render_prompt
    config = load_skill_config("comment_lead_screening")
    text = render_prompt(config, {"video_context_json": "{}",
                                  "comments_json": "[]",
                                  "comment_count": "0"})
    assert "is_car_owner" in text
    assert "has_purchase_intent" in text
    assert "positive_attitude" in text
    assert "comment_actor" in text
    assert "owner_status" not in text          # 旧枚举字段退场
    # 车主判定反例约束
    assert "准备订" in text or "想下定" in text
    # 车主意向词例与非本人意向规则
    assert "置换" in text
    assert "我朋友想买" in text
    # no_purchase_intent 由代码合成，不得出现在 LLM 输出模板中
    assert "no_purchase_intent" not in text


def test_v13_pipeline_screening_version_bumped():
    from app.workflow.pipeline import COMMENT_SCREENING_SKILL, SKILL_VERSIONS
    assert SKILL_VERSIONS[COMMENT_SCREENING_SKILL] == "1.3"
```

- [ ] **Step 2: 运行确认失败**

Run: `python -m pytest tests/test_skill_executor.py -q`
Expected: FAIL（配置仍指向 v2）。

- [ ] **Step 3: 创建 `app/skills/prompts/comment_lead_screening_v3.txt`**

完整内容：

```text
你是汽车销售线索分析专家。请结合视频语境，逐条分析以下抖音评论，判断每条评论的购车线索价值、评论主体类型、车主状态与购车意向。

视频语境（JSON）：
$video_context_json

评论列表（共 $comment_count 条，JSON）：
$comments_json

判断购车意向时必须区分以下四个层次：
1. 正面情绪（如"好看""厉害""good"）——不代表购车意向；
2. 产品兴趣（如"内饰不错""颜色好看"）——有兴趣但未进入决策；
3. 潜在购车需求（如"和XX比怎么样""保养贵不贵"）——开始了解或比较；
4. 明确购车意向（如"落地多少钱""哪里能试驾""置换有补贴吗"）——接近交易或行动。

评论主体类型 comment_actor（五选一）：
- genuine_user：真实普通用户（默认）；
- bot_spam：批量刷屏水军——短句复读、模板化口号、纯表情/数字灌水、与多条评论雷同；
- marketing_account：营销号/广告引流——含联系方式、导流话术、号召他人购买、软广模板化夸赞；
- noise："厉害""哈哈""排面"等无意义或纯情绪内容；
- off_topic：与视频和汽车均无关的内容。

是否车主 is_car_owner（布尔，独立判断，不要求是我方或视频中的车型）：
- true：有明确证据表明发布者大概率已购车（已下单/下大定也算已购车），
  如"提车三个月""我这台开了2万公里""大定已下""开了5年想换车"；
- false：无明确已购证据时一律 false。
车主判断的反例（必须判 false）：
- "准备订""想下定""打算买"是购买意向，不是已购/已定；
- 咨询他人用车体验（如"车主们保养贵不贵""已提车的朋友说说油耗"）是潜在买家，不是车主。

购车意向 has_purchase_intent（布尔，独立判断，不要求意向指向特定车型）：
- 车主的意向表达 → true：增换购类（换车、再买、添一台、置换、升级、入手、
  增换、二胎车等）、处置意图类（卖了、出手、保值率等）、抱怨不满类（不够用、
  空间小、开腻了、下一台、开了xx年等）；
- 非车主的意向表达 → true：询价、询问配置、对比、联系方式、优惠政策、等上市
  及其他类似的购买相关表述；
- 单纯夸赞、技术讨论（如"内饰好看""底盘不错"）不算购车意向 → false。

积极信号 positive_attitude（布尔）：非车主且无购车意向，但对车辆表达了兴趣、
赞美等积极信号（如"内饰好看""这底盘调校不错"）时为 true；其余情况为 false。

意向主体判断（重要）：购车意向必须是发布者本人的意向：
- 明确替他人问询/转述他人意向（如"我朋友想买一辆xxx"）→ 不作为本人购车信号，
  has_purchase_intent=false；
- 疑似怂恿他人购买/营销推广口吻（如选车视频下评论"分期30期买XX"，看不出是
  自己想买）→ 降低 intent_strength 并在 reason 中说明；营销特征明显时
  comment_actor=marketing_account。

注意事项：
- 负面情绪中包含真实换车需求（如"现在这车售后太差，准备换品牌"）属于有价值线索，不得过滤；
- 疑似车主口吻但同时带引流/广告特征的，comment_actor 判为 marketing_account；
- 旧字段 is_meaningful / is_suspected_marketing / is_automotive_related 仍需按原义输出，
  并与 comment_actor 保持一致（如 comment_actor=noise 时 is_meaningful=false）。
- 一条评论同时符合多个异常类型时，comment_actor 按 off_topic > noise > bot_spam > marketing_account 的顺序取最先命中项；

intent_signals 可选值：price_inquiry（询价）、trade_in（置换）、test_drive（试驾）、
finance（金融政策）、store_visit（门店/交付）、comparison（竞品对比）、
config_inquiry（配置咨询）、cost_concern（用车成本）、purchase_plan（购车计划）。

请严格输出以下 JSON（不要输出任何其他内容）。items 数组必须与输入评论一一对应：
数量相同、comment_id 完全一致、不得遗漏或新增：
{
  "items": [
    {
      "comment_id": "输入中的 comment_id，原样返回",
      "is_meaningful": true,
      "is_automotive_related": true,
      "is_purchase_related": true,
      "is_suspected_marketing": false,
      "comment_actor": "genuine_user | bot_spam | marketing_account | noise | off_topic",
      "is_car_owner": false,
      "has_purchase_intent": true,
      "positive_attitude": false,
      "intent_signals": ["price_inquiry"],
      "target_brand": "评论指向的品牌，没有则用视频语境品牌，无法判断为 null",
      "target_model": "评论指向的车型，同上",
      "intent_strength": "none | low | medium | high",
      "reason": "判断理由，一句话",
      "confidence": 0.9
    }
  ]
}
```

- [ ] **Step 4: 更新 `app/skills/configs/comment_lead_screening.yaml`**

```yaml
skill_id: comment_lead_screening
version: "1.3"
description: >
  结合视频语境批量判断评论的购车线索价值：识别无意义评论、营销内容、
  水军，独立判定"是否车主"与"购车意向"两标签及非车主积极信号，
  区分本人意向与替他人问询/怂恿他人购买。
model:
  name: ""
  temperature: 0.1
prompt_file: comment_lead_screening_v3.txt
prompt_version: "v3"
```

- [ ] **Step 5: 更新 `app/workflow/pipeline.py` 的 `SKILL_VERSIONS`**

```python
SKILL_VERSIONS = {
    VIDEO_CONTEXT_SKILL: "1.1",
    COMMENT_SCREENING_SKILL: "1.3",
    USER_ANALYSIS_SKILL: "1.2.1",
}
```

（`USER_ANALYSIS_SKILL` 在 Task 6 升版。）

- [ ] **Step 6: 全量测试**

Run: `python -m pytest tests/ -q`
Expected: 全部 PASS。

- [ ] **Step 7: 提交**

```powershell
git add -A; git commit -m @'
feat(v1.3): 初筛提示词 v3——两标签判定与非本人意向识别

comment_lead_screening 升版 1.3/v3：新增 is_car_owner/
has_purchase_intent/positive_attitude 判定指引与输出，移除
owner_status，加入意向主体判断规则（替他人问询/怂恿他人购买
不作本人购车信号）。SKILL_VERSIONS 升版触发重分析。

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
'@
```

---

### Task 4: 通过判定统一（8000 流水线口径 + Web 分类适配）

**Files:**
- Modify: `app/api/mapping.py`（新增 `screening_dict_passed` / `screened_out_category`）
- Modify: `app/services/aggregation.py:23-25`
- Modify: `app/services/leads.py:77-118`
- Modify: `app/templates/leads.html:121-150`、`app/templates/export_leads.html:193-222`
- Modify: `tests/test_aggregation.py`
- Test: `tests/test_api_mapping.py`（追加）

**Interfaces:**
- Consumes: Task 2 的 `resolve_filter_type` 与 `CommentScreeningItem`。
- Produces:
  - `screening_dict_passed(res: dict) -> bool`——对落库初筛结果 dict 判定是否通过（新数据走 V1.3 规则，含 `has_purchase_intent` 键缺失时回退旧口径 `is_purchase_related and not is_suspected_marketing`）。
  - `screened_out_category(res: dict) -> str`——返回 `"marketing" | "no_intent" | "unrelated"`，供 Web 未通过评论分类展示。

- [ ] **Step 1: 写失败测试（`tests/test_api_mapping.py` 追加）**

```python
def test_screening_dict_passed_v13_rules():
    from app.api.mapping import screening_dict_passed
    # 新数据（含 has_purchase_intent 键）走 V1.3 规则
    assert screening_dict_passed(
        {"comment_id": "c", "is_meaningful": True,
         "has_purchase_intent": True, "is_car_owner": True}) is True
    assert screening_dict_passed(
        {"comment_id": "c", "is_meaningful": True,
         "has_purchase_intent": False, "is_car_owner": True,
         "positive_attitude": True}) is False   # 无意向车主
    assert screening_dict_passed(
        {"comment_id": "c", "is_meaningful": True,
         "has_purchase_intent": False, "is_car_owner": False,
         "positive_attitude": True}) is True    # 非车主积极信号
    assert screening_dict_passed(
        {"comment_id": "c", "is_meaningful": True,
         "comment_actor": "marketing_account",
         "has_purchase_intent": True}) is False  # actor 异常优先


def test_screening_dict_passed_legacy_fallback():
    from app.api.mapping import screening_dict_passed
    # 历史数据（无 has_purchase_intent 键）回退旧口径
    assert screening_dict_passed(
        {"comment_id": "c", "is_purchase_related": True,
         "is_suspected_marketing": False}) is True
    assert screening_dict_passed(
        {"comment_id": "c", "is_purchase_related": False}) is False
    assert screening_dict_passed(
        {"comment_id": "c", "is_purchase_related": True,
         "is_suspected_marketing": True}) is False


def test_screened_out_category():
    from app.api.mapping import screened_out_category
    assert screened_out_category(
        {"comment_id": "c", "is_meaningful": True,
         "comment_actor": "marketing_account"}) == "marketing"
    assert screened_out_category(
        {"comment_id": "c", "is_meaningful": True,
         "has_purchase_intent": False, "is_car_owner": True}) == "no_intent"
    assert screened_out_category(
        {"comment_id": "c", "is_meaningful": False,
         "has_purchase_intent": False}) == "unrelated"
    # 历史数据回退
    assert screened_out_category(
        {"comment_id": "c", "is_suspected_marketing": True}) == "marketing"
    assert screened_out_category(
        {"comment_id": "c", "is_purchase_related": False}) == "unrelated"
```

- [ ] **Step 2: 写 8000 口径失败测试（`tests/test_aggregation.py` 追加）**

```python
def _screening_v13(cid, *, owner=False, intent=True, positive=False):
    return {"comment_id": str(cid), "is_meaningful": True,
            "is_automotive_related": True, "is_purchase_related": intent,
            "is_suspected_marketing": False,
            "comment_actor": "genuine_user", "is_car_owner": owner,
            "has_purchase_intent": intent, "positive_attitude": positive,
            "target_brand": "坦克", "target_model": "坦克300",
            "intent_strength": "high" if intent else "none",
            "reason": "r", "confidence": 0.9}


def test_candidate_users_v13_owner_without_intent_excluded(session):
    v = Video(platform="douyin", external_id="v1", title="t")
    u1 = PlatformUser(platform="douyin", external_id="u1", nickname="车主")
    u2 = PlatformUser(platform="douyin", external_id="u2", nickname="兴趣用户")
    session.add_all([v, u1, u2]); session.flush()
    c1 = Comment(platform="douyin", external_id="c1", video_id=v.id,
                 user_id=u1.id, content="我这台油耗8个")
    c2 = Comment(platform="douyin", external_id="c2", video_id=v.id,
                 user_id=u2.id, content="内饰真好看")
    session.add_all([c1, c2]); session.flush()
    sv = SKILL_VERSIONS[COMMENT_SCREENING_SKILL]
    # u1：车主无意向 → 不入候选；u2：非车主积极信号 → 入候选
    save_result(session, target_type="comment", target_id=str(c1.id),
                skill_id=COMMENT_SCREENING_SKILL, skill_version=sv,
                result=_screening_v13(c1.id, owner=True, intent=False))
    save_result(session, target_type="comment", target_id=str(c2.id),
                skill_id=COMMENT_SCREENING_SKILL, skill_version=sv,
                result=_screening_v13(c2.id, intent=False, positive=True))
    assert candidate_user_ids(session) == [u2.id]
```

（文件已有的 `_screening` 旧字段测试保留——它们验证历史数据回退路径。）

- [ ] **Step 3: 运行确认失败**

Run: `python -m pytest tests/test_api_mapping.py tests/test_aggregation.py -q`
Expected: FAIL（函数不存在 / 候选口径未变）。

- [ ] **Step 4: 在 `app/api/mapping.py` 实现共享判定**

在 `resolve_filter_type` 之后追加：

```python
def screening_dict_passed(res: dict) -> bool:
    """对落库的初筛结果 dict 判定是否通过初筛（8000 与 8100 同口径）。

    V1.3 之前的历史结果（无 has_purchase_intent 键）回退旧口径。
    """
    if "has_purchase_intent" not in res:
        return bool(res.get("is_purchase_related")
                    and not res.get("is_suspected_marketing"))
    item = CommentScreeningItem.model_validate(res)
    return resolve_filter_type(item) == "genuine_user"


def screened_out_category(res: dict) -> str:
    """未通过评论的展示分类：marketing / no_intent / unrelated。"""
    if "has_purchase_intent" not in res:
        return ("marketing" if res.get("is_suspected_marketing")
                else "unrelated")
    item = CommentScreeningItem.model_validate(res)
    ft = resolve_filter_type(item)
    if ft in ("marketing_account", "bot_spam"):
        return "marketing"
    if ft == "no_purchase_intent":
        return "no_intent"
    return "unrelated"
```

- [ ] **Step 5: 改造 `app/services/aggregation.py`**

导入 `from app.api.mapping import screening_dict_passed`，`_valid_screenings` 末尾（23-25 行）改为：

```python
    return [r for r in latest.values() if screening_dict_passed(r.result)]
```

- [ ] **Step 6: 改造 `app/services/leads.py`**

导入 `from app.api.mapping import screened_out_category, screening_dict_passed`；`_screening_passed`（77-79 行）删除，两处调用（89、153 行）改用 `screening_dict_passed(res)`；`query_screened_out_comments` 的 category 赋值（111-112 行）改为：

```python
            "category": screened_out_category(res),
```

docstring（85-87 行）改为：`含三类：疑似营销水军（marketing）、无购买倾向（no_intent）、与购车无关（unrelated）。` 排序 lambda（117-118 行）改为 marketing 在前、no_intent 次之、unrelated 最后：

```python
    order = {"marketing": 0, "no_intent": 1, "unrelated": 2}
    return sorted(rows, key=lambda d: (order.get(d["category"], 9),
                                       -(d["confidence"] or 0)))
```

- [ ] **Step 7: 更新两处模板的分类展示**

`app/templates/leads.html` 121-150 行的统计与判定列改为三类：

```html
  {% set n_marketing = screened_out | selectattr("category", "equalto",
                                                 "marketing") | list | length %}
  {% set n_no_intent = screened_out | selectattr("category", "equalto",
                                                 "no_intent") | list | length %}
  {% set n_unrelated = screened_out | length - n_marketing - n_no_intent %}
```

hint 行改为：`（疑似水军 {{ n_marketing }} · 无购买倾向 {{ n_no_intent }} · 非购车相关 {{ n_unrelated }}）`。判定列改为：

```html
            {% if c.category == "marketing" %}
            <span class="pill pill-invalid">疑似水军</span>
            {% elif c.category == "no_intent" %}
            <span class="pill">无购买倾向</span>
            {% else %}
            <span class="pill">非购车相关</span>
            {% endif %}
```

`app/templates/export_leads.html` 193-222 行做同样的三类改造（set 三个计数、标题文案、判定列 if/elif/else）。

- [ ] **Step 8: 全量测试**

Run: `python -m pytest tests/ -q`
Expected: 全部 PASS（`test_e2e.py` 与旧 `_screening` 数据走回退路径，保持绿灯）。

- [ ] **Step 9: 提交**

```powershell
git add -A; git commit -m @'
feat(v1.3): 统一初筛通过判定口径，Web 未通过评论增加无购买倾向分类

- mapping 新增 screening_dict_passed / screened_out_category 共享函数
- 8000 流水线候选用户口径与 8100 API passed 一致，历史数据回退旧口径
- 未通过评论页三分类：疑似水军 / 无购买倾向 / 非购车相关

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
'@
```

---

### Task 5: Agent2 两标签（schema、映射、对外契约）

**Files:**
- Modify: `app/schemas/skills.py`（`UserLeadResult` 新增 2 字段）
- Modify: `app/api/schemas.py:70-80`（`ProfileResult` 新增 2 字段）
- Modify: `app/api/mapping.py:54-73`（`map_profile_result` 透出）
- Modify: `app/api/agent2.py:100-105`（错误分支补 null）
- Modify: `tests/test_api_skill_schema.py`、`tests/test_api_mapping.py`、`tests/test_agent2.py`

**Interfaces:**
- Consumes: 现有 `map_profile_result(out, *, screenshot_available, has_comments, processed_at)`。
- Produces:
  - `UserLeadResult` 新字段：`is_car_owner: bool = False`、`has_purchase_intent: bool = False`。
  - `ProfileResult` 新字段：`is_car_owner: bool | None = None`、`has_purchase_intent: bool | None = None`（错误条目为 null；`has_value=false` 分支也透出）。

- [ ] **Step 1: 写失败测试**

`tests/test_api_skill_schema.py` 追加：

```python
def test_user_lead_result_v13_label_fields():
    out = UserLeadResult(lead_grade="A", is_car_owner=True,
                         has_purchase_intent=True)
    assert out.is_car_owner is True
    assert out.has_purchase_intent is True


def test_user_lead_result_v13_label_defaults():
    out = UserLeadResult(lead_grade="C")
    assert out.is_car_owner is False
    assert out.has_purchase_intent is False
```

`tests/test_api_mapping.py` 追加：

```python
def test_map_profile_carries_labels():
    out = UserLeadResult(lead_grade="H", is_valid_lead=True, confidence=0.9,
                         is_car_owner=True, has_purchase_intent=True)
    r = map_profile_result(out, screenshot_available=True, has_comments=True,
                           processed_at="t")
    assert r.is_car_owner is True
    assert r.has_purchase_intent is True


def test_map_profile_no_value_still_carries_labels():
    # has_value=false（如 C 级）仍透出两标签
    out = UserLeadResult(lead_grade="C", is_valid_lead=True,
                         is_car_owner=True, has_purchase_intent=False)
    r = map_profile_result(out, screenshot_available=True, has_comments=True,
                           processed_at="t")
    assert r.has_value is False
    assert r.is_car_owner is True
    assert r.has_purchase_intent is False
```

`tests/test_agent2.py` 追加（放在文件末尾，沿用文件内 `_executor_and_gateway`）：

```python
@pytest.mark.asyncio
async def test_v13_profile_result_carries_labels():
    lead = json.dumps({
        "lead_grade": "A", "is_valid_lead": True, "lead_summary": "s",
        "evidence_comment_ids": ["x"], "confidence": 0.8,
        "is_car_owner": True, "has_purchase_intent": True,
        "profile_tags": [], "profile_summary": "p", "analysis_text": "a"})
    executor, gateway = _executor_and_gateway(lead)
    req = ProfileAnalysisRequest(accounts=[{
        "account_uid": "u9", "account_name": "用户",
        "account_homepage_screenshot": "",
        "comment_history": [{"video_title": "t", "comment_content": "想置换",
                             "comment_time": "2026-07-19T14:23:00+08:00",
                             "comment_like_count": 1}]}])
    out = await run_profile_analysis(executor, gateway, req)
    r = out["results"][0]
    assert r["is_car_owner"] is True
    assert r["has_purchase_intent"] is True


@pytest.mark.asyncio
async def test_v13_profile_error_item_labels_null():
    # LLM 无响应 → 该账号处理失败，两标签为 null
    executor, gateway = _executor_and_gateway()
    req = ProfileAnalysisRequest(accounts=[{
        "account_uid": "u10", "account_name": "用户",
        "account_homepage_screenshot": "",
        "comment_history": [{"video_title": "t", "comment_content": "c",
                             "comment_time": "2026-07-19T14:23:00+08:00",
                             "comment_like_count": 1}]}])
    out = await run_profile_analysis(executor, gateway, req)
    r = out["results"][0]
    assert r["error"]
    assert r["is_car_owner"] is None
    assert r["has_purchase_intent"] is None
```

- [ ] **Step 2: 运行确认失败**

Run: `python -m pytest tests/test_api_skill_schema.py tests/test_api_mapping.py tests/test_agent2.py -q`
Expected: FAIL。

- [ ] **Step 3: 实现**

`app/schemas/skills.py` 的 `UserLeadResult`，在 `confidence` 之前追加：

```python
    # V1.3：账号级独立标签，综合全部历史评论与主页画像判定。
    # 无评论历史（has_value=false 空账号）时保持默认 false。
    is_car_owner: bool = False
    has_purchase_intent: bool = False
```

`app/api/schemas.py` 的 `ProfileResult`，在 `profile_tags` 之前插入：

```python
    # V1.3：独立分析标签；该条处理失败时为 null
    is_car_owner: bool | None = None
    has_purchase_intent: bool | None = None
```

`app/api/mapping.py::map_profile_result` 两个 `ProfileResult(...)` 构造均加入：

```python
            is_car_owner=out.is_car_owner,
            has_purchase_intent=out.has_purchase_intent,
```

`app/api/agent2.py` 错误分支 dict（101-105 行）加入 `"is_car_owner": None, "has_purchase_intent": None,`。

- [ ] **Step 4: 全量测试**

Run: `python -m pytest tests/ -q`
Expected: 全部 PASS。

- [ ] **Step 5: 提交**

```powershell
git add -A; git commit -m @'
feat(v1.3): Agent2 账号级车主/购车意向标签落库与对外透出

UserLeadResult/ProfileResult 新增 is_car_owner/has_purchase_intent，
map_profile_result 全分支透出（含 has_value=false），错误条目为 null。

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
'@
```

---

### Task 6: 用户分析提示词 v5 与技能升版

**Files:**
- Create: `app/skills/prompts/user_lead_analysis_v5.txt`
- Modify: `app/skills/configs/user_lead_analysis.yaml`
- Modify: `app/workflow/pipeline.py`（`SKILL_VERSIONS` 用户分析升 1.3）
- Modify: `tests/test_agent2.py:70-75, 308-316`

**Interfaces:**
- Consumes: Task 5 的 `UserLeadResult` 新字段（LLM 输出 JSON 键对齐）。
- Produces: `user_lead_analysis` 技能 version="1.3"、prompt_version="v5"；`SKILL_VERSIONS[USER_ANALYSIS_SKILL] == "1.3"`。占位符与 v4 相同：`$user_evidence_json` / `$grading_standard` / `$our_models_summary`。

- [ ] **Step 1: 写失败测试（`tests/test_agent2.py`）**

`test_v121_user_analysis_config_uses_v4`（70-75 行）改为：

```python
def test_v13_user_analysis_config_uses_v5():
    from app.skills.executor import load_skill_config
    config = load_skill_config("user_lead_analysis")
    assert config.prompt_file == "user_lead_analysis_v5.txt"
    assert config.prompt_version == "v5"
    assert config.version == "1.3"
```

`test_v121_pipeline_skill_version_bumped`（308-316 行）中对 `USER_ANALYSIS_SKILL` 的断言值改为 `"1.3"`。追加：

```python
def test_v13_user_analysis_prompt_has_label_rules():
    from app.skills.executor import load_skill_config, render_prompt
    config = load_skill_config("user_lead_analysis")
    text = render_prompt(config, {
        "user_evidence_json": "{}",
        "grading_standard": "标准",
        "our_models_summary": "- 方舟X7：售价 35-42 万元"})
    assert "is_car_owner" in text
    assert "has_purchase_intent" in text
    assert "我朋友想买" in text          # 非本人意向规则
    assert "置换" in text                # 车主意向词例
    # v4 三段流水线与审计字段原样保留
    assert "baseline_grade" in text
    assert "model_match_level" in text
    assert "只上调" in text
```

- [ ] **Step 2: 运行确认失败**

Run: `python -m pytest tests/test_agent2.py -q`
Expected: FAIL（配置仍指向 v4）。

- [ ] **Step 3: 创建 `app/skills/prompts/user_lead_analysis_v5.txt`**

以 v4 全文为底，做三处修改（其余内容与 v4 逐字一致）：

（1）在"我方在售主力车型信息：$our_models_summary"段之后、"评级流程分三段流水线"段之前，插入：

```text
【独立标签】综合该用户全部评论与主页画像，先给出两个独立布尔标签
（相互独立、与评级并行，不要求与我方在售车型相关）：
- is_car_owner（是否车主）：有明确证据表明该用户大概率已购车
  （已下单/下大定也算已购车，不要求是我方在售车型）→ true，否则 false。
  证据示例："提车三个月""我这台开了2万公里""大定已下""开了5年想换车"。
  反例（判 false）："准备订""想下定""打算买"是意向不是已购；
  咨询他人用车体验（如"车主们保养贵不贵"）是潜在买家，不是车主。
- has_purchase_intent（购车意向）：表达了任何买车相关倾向 → true，否则 false。
  车主的意向表达：增换购类（换车、再买、添一台、置换、升级、入手、二胎车等）、
  处置意图类（卖了、出手、保值率等）、抱怨不满类（不够用、空间小、开腻了、
  下一台、开了xx年等）；
  非车主的意向表达：询价、询问配置、对比、联系、优惠政策、等上市等。
  单纯夸赞、技术讨论（如"内饰好看""底盘不错"）不算购车意向。

【意向主体判断（重要）】购车意向必须是该用户本人的意向：
- 明确替他人问询/转述他人意向（如"我朋友想买一辆xxx"）→ 不作为本人购车信号，
  has_purchase_intent=false，相关评论不得作为评级的意向证据；
- 疑似怂恿他人购买/营销推广口吻（如选车视频下评论"分期30期买XX"，看不出是
  自己想买）→ 评级降档处理并在 analysis_text 中说明；营销特征明显时
  is_valid_lead=false。
```

（2）输出 JSON 中，在 `"baseline_grade"` 行之前插入两行：

```text
  "is_car_owner": "是否车主布尔值：true | false",
  "has_purchase_intent": "是否有购车意向布尔值：true | false",
```

（3）`analysis_text` 一行的描述尾部追加：`，两个独立标签的判定依据写入评论行为段`。

- [ ] **Step 4: 更新 `app/skills/configs/user_lead_analysis.yaml`**

```yaml
skill_id: user_lead_analysis
version: "1.3"
description: >
  基于用户全部有效评论、视频语境与统计特征，综合判断购车意向，
  输出 H/A/B/C 等级、购车画像与销售建议；独立判定账号级"是否车主"
  与"购车意向"标签；评级采用三段流水线：评论基线 → 在售车型匹配度
  调整（四档）→ 主页画像有限上调，并识别非本人意向予以降档。
model:
  name: ""
  temperature: 0.1
prompt_file: user_lead_analysis_v5.txt
prompt_version: "v5"
```

- [ ] **Step 5: 更新 `app/workflow/pipeline.py` 的 `SKILL_VERSIONS`**

`USER_ANALYSIS_SKILL: "1.2.1"` → `USER_ANALYSIS_SKILL: "1.3"`。

- [ ] **Step 6: 全量测试**

Run: `python -m pytest tests/ -q`
Expected: 全部 PASS（`test_agent2.py` 中其余 v12x prompt 测试断言的 `baseline_grade`/`match_adjustment`/画像规则文本在 v5 中原样保留，应继续通过）。

- [ ] **Step 7: 提交**

```powershell
git add -A; git commit -m @'
feat(v1.3): 用户分析提示词 v5——账号级两标签与非本人意向降档

user_lead_analysis 升版 1.3/v5：新增 is_car_owner/has_purchase_intent
账号级判定与输出、非本人意向识别规则（替他人问询不作证据、怂恿
口吻降档），v4 三段评级流水线原样保留。SKILL_VERSIONS 升版触发重分析。

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
'@
```

---

### Task 7: API 对接文档更新与全量回归

**Files:**
- Modify: `docs/DriveIntent-V1-API对接文档.md`

**Interfaces:**
- Consumes: Task 2/5 的对外字段定义、Task 2 的 filter_type 枚举表。
- Produces: 对接文档 1.3 版。

- [ ] **Step 1: 更新文档头**

版本号 `1.2.1` → `1.3`，更新日期 → `2026-08-03`。

- [ ] **Step 2: 更新 Agent1 结果字段表（109-119 行区域）**

字段表在 `filter_type` 行之后加两行：

```markdown
| `is_car_owner` | Boolean \| null | 是否车主：有明确证据表明发布者大概率已购车（已下单/下大定也算），**不要求**是我方在售车型；该条处理失败时为 `null` |
| `has_purchase_intent` | Boolean \| null | 购车意向：是否表达了任何买车相关倾向（本人意向，**不要求**指向我方在售车型）；该条处理失败时为 `null` |
```

`filter_reason` 行说明改为：`filter_type 的纯文本补充说明，V1.3 起恒为 null（保留字段，对接方无需解析）`。

- [ ] **Step 3: 替换 filter_type 枚举表（121-139 行区域）**

```markdown
`filter_type` 枚举与 `passed` 对应关系（V1.3 起严格一一对应）：

| filter_type | 含义 | passed |
|---|---|---|
| `genuine_user` | 真实用户，有购车意向或积极信号，通过初筛 | `true` |
| `no_purchase_intent` | 提到汽车相关内容但无购车意向/积极信号（车主纯讨论吐槽、非车主无兴趣表达） | `false` |
| `bot_spam` | 批量刷屏水军 | `false` |
| `marketing_account` | 营销号/广告引流 | `false` |
| `noise` | 无实质内容 | `false` |
| `off_topic` | 与汽车无关 | `false` |

**V1.3 契约变更说明**（相对 V1.2.1）：
- 每条结果新增 `is_car_owner`、`has_purchase_intent` 两个独立布尔字段，Agent 2 账号结果同步新增（见 Agent 2 节）。
- **移除** `model_mismatch` / `existing_owner` / `ordered_owner` 三个枚举值：初筛不再考虑车型匹配；车主状态改由独立字段 `is_car_owner` 表达。
- `passed` 与 `filter_type` 恢复严格一一对应，不再有 `model_mismatch` 这种 `passed=true` 的标记型例外。
- 车主评论不再一刀切过滤：车主若表达增换购/处置意向则 `has_purchase_intent=true` 并通过初筛。
- 无购车意向的非车主若表达兴趣/赞美可通过初筛（`genuine_user` + `has_purchase_intent=false`），对接方可据两布尔字段将其识别为 B 级弱线索。
- 初筛与画像分析均会识别"非本人意向"（替他人问询、怂恿他人购买、营销推广口吻），不作为本人购车信号或予以降级。
- 请求侧（入参）无变化。
```

删除原"V1.1.1 契约变更说明"与"降级功能的服务端前提"两段（134-139 行区域）。

- [ ] **Step 4: 更新 Agent2 结果字段表（177-190 行区域）**

在 `value_score` 行之后加两行：

```markdown
| `is_car_owner` | Boolean \| null | 是否车主（综合该账号全部历史评论与主页画像判定，口径同 Agent 1）；该条处理失败时为 `null` |
| `has_purchase_intent` | Boolean \| null | 购车意向（综合判定，口径同 Agent 1）；该条处理失败时为 `null` |
```

并在 Agent2 节的评级说明处（若有"评级考虑意向车型与在售车型匹配度"相关文字）保持不变——V1.3 定级方式与 V1.2.1 一致。

- [ ] **Step 5: 全量回归**

Run: `python -m pytest tests/ -q`
Expected: 全部 PASS。

- [ ] **Step 6: 提交**

```powershell
git add -A; git commit -m @'
docs(v1.3): API 对接文档升版 1.3——两标签字段与 filter_type 枚举变更

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
'@
```
