# V1.6.4 实施计划：analysis 润色节点 + 复核接入 API 路径

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在定级+复核之后新增 LLM 润色节点统一重写三个对外叙述字段，并把复核节点补接入对外 API 路径，消除 analysis 的英文字段泄漏与定级矛盾。

**Architecture:** 复核逻辑从 `pipeline.py` 抽出为共享模块 `app/skills/user_review.py`，新增润色共享模块 `app/skills/analysis_polish.py`，V0 流水线路径与对外 API 路径统一为「过滤 → 定级 → 复核 → 润色」节点序列。润色 fail-open、只改文本不改级，新增 `analysis_polish` 审计字段保证可观测性。设计文档：`claude_docs/versions/V1/V1.6/v1.6.4-design.md`（已批准）。

**Tech Stack:** Python / FastAPI / SQLAlchemy / Pydantic / pytest（`asyncio_mode = auto`，async 测试无需装饰器）/ MockProvider（FIFO 响应队列）。

## Global Constraints

- 简体中文写文档与注释；**每次向文件写入中文后必须读回检查是否乱码**（CLAUDE.md 临时要求），乱码须立即修复。
- 代码风格与现有代码一致：行宽约 79 列、中文注释只写代码无法表达的约束。
- Prompt 文件经 `string.Template.substitute` 渲染：**正文除占位符外不得出现裸 `$`**（会抛 KeyError/ValueError）。
- MockProvider 是 FIFO 队列：每个 LLM 节点恰好消费一个响应；**队列长度必须与节点调用数严格一致**，多排会错位、少排会被 fail-open 吞掉（V1.6.2 教训：复核路径长期零覆盖却全绿）。集成测试必须断言审计字段证明节点真实走到。
- 对外 API 契约（`ProfileResult`）、DB schema、映射逻辑一律不动。
- 定级 Prompt（`user_lead_analysis_v1.6.3.txt`）、复核 Prompt（`user_lead_review_v1.6.3.txt`）内容不动、不重命名。
- 五个段标题字面量（锚点，与定级 Prompt 逐字一致）：`一、评论行为与用户身份` / `二、购车阶段评估` / `三、目标车型与我方车型匹配度` / `四、主页画像与调整结论` / `五、总体评价`。
- 测试命令统一 `python -m pytest tests/... -v`（仓库根目录执行）。
- 每个任务结束提交一次 git；提交信息末尾加 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`。

---

### Task 1: Schema——AnalysisPolishResult 与 analysis_polish 审计字段

**Files:**
- Modify: `app/schemas/skills.py`
- Test: `tests/test_analysis_polish.py`（新建）

**Interfaces:**
- Consumes: 无（纯 schema）。
- Produces: `AnalysisPolishResult`（字段 `polished_analysis_text: str = ""`、`polished_lead_summary: str = ""`、`polished_profile_summary: str = ""`、`confidence: float = 0.0`）；`UserLeadResult.analysis_polish: str = "none"`。后续任务按这些名字使用。

- [ ] **Step 1: 写失败测试**

新建 `tests/test_analysis_polish.py`：

```python
"""V1.6.4：analysis 润色节点。

定级+复核之后统一润色三个对外叙述字段，清除英文字段泄漏、消除与最终
定级的矛盾。fail-open：失败保留原文；只改文本不改级。
"""
import json


def test_v164_analysis_polish_result_defaults():
    from app.schemas.skills import AnalysisPolishResult
    r = AnalysisPolishResult()
    assert r.polished_analysis_text == ""
    assert r.polished_lead_summary == ""
    assert r.polished_profile_summary == ""
    assert r.confidence == 0.0


def test_v164_user_lead_result_analysis_polish_default():
    from app.schemas.skills import UserLeadResult
    assert UserLeadResult(lead_grade="C").analysis_polish == "none"
```

- [ ] **Step 2: 运行确认失败**

Run: `python -m pytest tests/test_analysis_polish.py -v`
Expected: FAIL，`ImportError: cannot import name 'AnalysisPolishResult'`

- [ ] **Step 3: 实现 schema**

`app/schemas/skills.py` 两处修改。

其一：`UserLeadResult` 中 `analysis_revision` 字段（约 104 行）之后、`filter_category` 注释之前插入：

```python
    # V1.6.4：润色节点审计字段。none=未走润色（被过滤账号）；
    # polished=已应用（含 summary 单字段为空、部分应用）；
    # failed=调用失败或输出非法（为空/缺段标题），保留原文。
    # failed 占比偏高说明润色 Prompt 输出格式不稳，需加强约束。
    analysis_polish: str = "none"         # none | polished | failed
```

并把 `analysis_revision` 上方审计链条注释的末行

```python
    #              ->(analysis_revision)-> 对外叙述文本（V1.6.3）
```

改为：

```python
    #              ->(analysis_revision)-> 锚点修订后叙述（V1.6.3）
    #              ->(analysis_polish)-> 对外叙述文本（V1.6.4）
```

其二：`UserLeadReviewResult` 类之后新增：

```python
class AnalysisPolishResult(BaseModel):
    """V1.6.4 润色节点输出。

    对定级+复核后的三个对外叙述字段做一次专门润色。调用方校验
    polished_analysis_text 非空且五段标题齐全后才应用（fail-open）。
    """
    polished_analysis_text: str = ""
    polished_lead_summary: str = ""
    polished_profile_summary: str = ""
    confidence: float = 0.0
```

- [ ] **Step 4: 运行确认通过**

Run: `python -m pytest tests/test_analysis_polish.py -v`
Expected: 2 passed

- [ ] **Step 5: 读回 `app/schemas/skills.py` 新增部分检查中文无乱码；全量回归**

Run: `python -m pytest tests/ -q`
Expected: 全部通过（既有 213+ 用例无回归）

- [ ] **Step 6: Commit**

```bash
git add app/schemas/skills.py tests/test_analysis_polish.py
git commit -m "feat(v1.6.4): 润色节点输出 schema 与 analysis_polish 审计字段"
```

---

### Task 2: 润色 Skill 资产——Prompt v1.6.4 与 YAML 配置

**Files:**
- Create: `app/skills/prompts/user_analysis_polish_v1.6.4.txt`
- Create: `app/skills/configs/user_analysis_polish.yaml`
- Test: `tests/test_analysis_polish.py`（追加）

**Interfaces:**
- Consumes: 无。
- Produces: skill_id `user_analysis_polish`，占位符 `$lead_grade` / `$review_action` / `$review_reason` / `$analysis_text` / `$lead_summary` / `$profile_summary`。Task 3 的 `apply_polish` 按这六个键构造 context。

- [ ] **Step 1: 写失败测试**

`tests/test_analysis_polish.py` 追加：

```python
def test_v164_polish_config():
    from app.skills.executor import load_skill_config
    config = load_skill_config("user_analysis_polish")
    assert config.version == "1.6.4"
    assert config.prompt_file == "user_analysis_polish_v1.6.4.txt"
    assert config.prompt_version == "v1.6.4"
    assert config.multimodal is False


def test_v164_polish_prompt_renders_with_all_placeholders():
    """六个占位符齐全，正文无裸 $（Template.substitute 不抛错即证明）。"""
    from app.skills.executor import load_skill_config, render_prompt
    config = load_skill_config("user_analysis_polish")
    text = render_prompt(config, {
        "lead_grade": "A", "review_action": "维持原级",
        "review_reason": "（无）", "analysis_text": "正文",
        "lead_summary": "速读", "profile_summary": "画像"})
    assert "正文" in text and "速读" in text and "画像" in text


def test_v164_polish_prompt_has_headings_and_bans():
    from app.skills.executor import load_skill_config, render_prompt
    config = load_skill_config("user_analysis_polish")
    text = render_prompt(config, {
        "lead_grade": "A", "review_action": "维持原级",
        "review_reason": "（无）", "analysis_text": "a",
        "lead_summary": "b", "profile_summary": "c"})
    for heading in ("一、评论行为与用户身份", "二、购车阶段评估",
                    "三、目标车型与我方车型匹配度", "四、主页画像与调整结论",
                    "五、总体评价"):
        assert heading in text
    assert "is_car_owner" in text        # 禁令反例展示
    assert "严禁新增原文没有的事实" in text
    assert "最终评级为准" in text
    assert "JSON" in text
```

- [ ] **Step 2: 运行确认失败**

Run: `python -m pytest tests/test_analysis_polish.py -v`
Expected: 新增 3 个用例 FAIL（`FileNotFoundError`：YAML 不存在）

- [ ] **Step 3: 写 Prompt 文件**

新建 `app/skills/prompts/user_analysis_polish_v1.6.4.txt`，内容如下（逐字）：

```
你是一位面向汽车销售团队的报告编辑。下面是一份销售线索分析报告的三个
文本字段，以及该线索的最终评级信息。请对文本做一次润色改写，提升可读性，
确保销售人员可以直接阅读、不产生歧义。

最终评级（已定案，不可更改）：$lead_grade
复核动作：$review_action
复核理由：$review_reason

待润色文本一（分析叙述正文）：
$analysis_text

待润色文本二（速读结论）：
$lead_summary

待润色文本三（账号画像摘要）：
$profile_summary

润色要求（严格遵守）：
1. 结构保持：分析叙述正文必须保持五段结构，五个段标题独占一行、
   逐字保留、不得改写或增删：
一、评论行为与用户身份
二、购车阶段评估
三、目标车型与我方车型匹配度
四、主页画像与调整结论
五、总体评价
   各段字数与原文相当；不得合并、删除或新增段落。
2. 事实保真：原文中的事实细节（车型名、评论内容引用、时间、数字等）
   逐一保留；严禁新增原文没有的事实，严禁改变事实性结论。
3. 清除英文泄漏：凡出现英文字段名、枚举值或布尔字面量（例如
   is_car_owner、profile_adjustment、similar、true、false），
   一律改写为自然中文业务表述；不得残留任何系统内部字段名。
   错误示例："is_car_owner 为 true；匹配档位为 similar"
   正确示例："该用户疑似已拥有车辆；其目标车型与我方车型同处一个细分市场"
   品牌/车型专有名词（如 Model Y）不在此列，应当保留。
4. 消除矛盾：全文的销售价值判断与跟进优先级必须与上述最终评级一致。
   若某处论证的等级与最终评级不符，以最终评级为准改写该论证；
   复核理由（如非空）可作为改写依据。不得自行另定等级。
5. 顺畅衔接：消除歧义、重复与前后不一致的表述；速读结论与账号画像摘要
   同受第 2、3、4 条约束。

严格按以下 JSON 格式输出（不含其他内容）：
{
  "polished_analysis_text": "润色后的分析叙述，保持五段结构与段标题",
  "polished_lead_summary": "润色后的速读结论，1-2 句",
  "polished_profile_summary": "润色后的账号画像摘要",
  "confidence": 0.9
}
```

- [ ] **Step 4: 写 YAML 配置**

新建 `app/skills/configs/user_analysis_polish.yaml`：

```yaml
skill_id: user_analysis_polish
version: "1.6.4"
description: >
  analysis 润色节点：对定级+复核后的三个对外叙述字段
  （analysis_text / lead_summary / profile_summary）做一次专门润色，
  清除英文字段名/枚举值/布尔字面量泄漏，消除叙述与最终定级的矛盾，
  提升可读性。只改文本不改级；fail-open，失败保留原文。
model:
  name: ""
  temperature: 0.1
prompt_file: user_analysis_polish_v1.6.4.txt
prompt_version: "v1.6.4"
```

- [ ] **Step 5: 读回两个新文件全文，检查中文无乱码**

- [ ] **Step 6: 运行确认通过**

Run: `python -m pytest tests/test_analysis_polish.py -v`
Expected: 全部通过

- [ ] **Step 7: Commit**

```bash
git add app/skills/prompts/user_analysis_polish_v1.6.4.txt app/skills/configs/user_analysis_polish.yaml tests/test_analysis_polish.py
git commit -m "feat(v1.6.4): 润色节点 Prompt 与 Skill 配置"
```

---

### Task 3: 润色共享模块 apply_polish

**Files:**
- Create: `app/skills/analysis_polish.py`
- Test: `tests/test_analysis_polish.py`（追加）

**Interfaces:**
- Consumes: Task 1 的 `AnalysisPolishResult`、`UserLeadResult.analysis_polish`；Task 2 的 skill 资产；`app.skills.executor.SkillExecutionError`。
- Produces: `async def apply_polish(executor, out: UserLeadResult) -> None`（就地修改 out）；常量 `ANALYSIS_POLISH_SKILL = "user_analysis_polish"`、`SECTION_HEADINGS`（五段标题 list）；测试常量 `ORIG` / `POLISHED` / `POLISH_OK_JSON`（供 Task 5/6 的集成测试 import 复用）。

- [ ] **Step 1: 写失败测试**

`tests/test_analysis_polish.py` 追加（模块顶部已有 `import json`）：

```python
# —— 供本文件与其他测试文件复用的样例与 Mock 响应 ——
ORIG = "\n".join([
    "一、评论行为与用户身份", "用户多次询价，is_car_owner 为 false。",
    "二、购车阶段评估", "处于积极对比阶段。",
    "三、目标车型与我方车型匹配度", "匹配档位为 similar，未做调整。",
    "四、主页画像与调整结论", "主页画像无支持上调的直接证据。",
    "五、总体评价", "综合判定为 A 级线索，建议常规跟进。"])
POLISHED = (ORIG
            .replace("is_car_owner 为 false", "尚未拥有车辆")
            .replace("匹配档位为 similar，未做调整",
                     "目标车型与我方车型同处一个细分市场，未做评级调整"))
POLISH_OK_JSON = json.dumps({
    "polished_analysis_text": POLISHED,
    "polished_lead_summary": "润色后的速读结论。",
    "polished_profile_summary": "润色后的画像摘要。",
    "confidence": 0.9}, ensure_ascii=False)


def _lead():
    from app.schemas.skills import UserLeadResult
    return UserLeadResult(lead_grade="A", analysis_text=ORIG,
                          lead_summary="原速读结论", profile_summary="原画像摘要")


def _executor(*responses):
    from app.llm.gateway import LLMGateway
    from app.llm.mock import MockProvider
    from app.skills.executor import SkillExecutor
    provider = MockProvider()
    provider.queue(*responses)
    return SkillExecutor(LLMGateway(provider))


async def test_v164_apply_polish_replaces_all_three_fields():
    from app.skills.analysis_polish import apply_polish
    out = _lead()
    await apply_polish(_executor(POLISH_OK_JSON), out)
    assert out.analysis_polish == "polished"
    assert out.analysis_text == POLISHED
    assert out.lead_summary == "润色后的速读结论。"
    assert out.profile_summary == "润色后的画像摘要。"
    assert out.lead_grade == "A"            # 只改文本不改级


async def test_v164_apply_polish_llm_failure_fail_open():
    from app.skills.analysis_polish import apply_polish
    out = _lead()
    await apply_polish(_executor(), out)    # 空队列 → LLMError
    assert out.analysis_polish == "failed"
    assert out.analysis_text == ORIG        # 三字段原文保留
    assert out.lead_summary == "原速读结论"
    assert out.profile_summary == "原画像摘要"


async def test_v164_apply_polish_empty_text_fail_open():
    from app.skills.analysis_polish import apply_polish
    bad = json.dumps({"polished_analysis_text": "",
                      "polished_lead_summary": "新速读",
                      "polished_profile_summary": "新画像",
                      "confidence": 0.9}, ensure_ascii=False)
    out = _lead()
    await apply_polish(_executor(bad), out)
    assert out.analysis_polish == "failed"
    assert out.analysis_text == ORIG
    assert out.lead_summary == "原速读结论"    # 整体不可信，一个都不换


async def test_v164_apply_polish_missing_heading_fail_open():
    from app.skills.analysis_polish import apply_polish
    broken = ORIG.replace("五、总体评价", "总结")   # 丢一个段标题
    bad = json.dumps({"polished_analysis_text": broken,
                      "polished_lead_summary": "新速读",
                      "polished_profile_summary": "新画像",
                      "confidence": 0.9}, ensure_ascii=False)
    out = _lead()
    await apply_polish(_executor(bad), out)
    assert out.analysis_polish == "failed"
    assert out.analysis_text == ORIG


async def test_v164_apply_polish_empty_summaries_partial_apply():
    from app.skills.analysis_polish import apply_polish
    partial = json.dumps({"polished_analysis_text": POLISHED,
                          "polished_lead_summary": "",
                          "polished_profile_summary": "  ",
                          "confidence": 0.9}, ensure_ascii=False)
    out = _lead()
    await apply_polish(_executor(partial), out)
    assert out.analysis_polish == "polished"
    assert out.analysis_text == POLISHED
    assert out.lead_summary == "原速读结论"      # 单字段为空则保留原值
    assert out.profile_summary == "原画像摘要"
```

- [ ] **Step 2: 运行确认失败**

Run: `python -m pytest tests/test_analysis_polish.py -v`
Expected: 新增 5 个用例 FAIL（`ModuleNotFoundError: app.skills.analysis_polish`）

- [ ] **Step 3: 实现模块**

新建 `app/skills/analysis_polish.py`：

```python
import logging

from app.schemas.skills import AnalysisPolishResult, UserLeadResult
from app.skills.executor import SkillExecutionError

logger = logging.getLogger(__name__)

ANALYSIS_POLISH_SKILL = "user_analysis_polish"

# 五个段标题，须与定级 Prompt（user_lead_analysis_v1.6.3.txt）逐字一致
SECTION_HEADINGS = ["一、评论行为与用户身份", "二、购车阶段评估",
                    "三、目标车型与我方车型匹配度", "四、主页画像与调整结论",
                    "五、总体评价"]

# 复核动作转译为中文，避免向润色 Prompt 输入英文枚举值
_REVIEW_ACTION_ZH = {"confirmed": "维持原级", "upgraded": "上调一级",
                     "downgraded": "下调一级"}


async def apply_polish(executor, out: UserLeadResult) -> None:
    """V1.6.4 润色节点：重写三个对外叙述字段，就地修改 out。

    fail-open：调用失败、输出为空或缺失段标题时保留原文并记 "failed"。
    只改文本不改级——lead_grade 与结构化字段一律不动。刻意不传用户
    证据包：润色是纯文本改写，给证据包会诱导模型引入新事实。
    """
    ctx = {
        "lead_grade": out.lead_grade,
        "review_action": _REVIEW_ACTION_ZH.get(out.review_action,
                                               out.review_action),
        "review_reason": out.review_reason or "（无）",
        "analysis_text": out.analysis_text,
        "lead_summary": out.lead_summary,
        "profile_summary": out.profile_summary,
    }
    try:
        res: AnalysisPolishResult = await executor.run(
            ANALYSIS_POLISH_SKILL, ctx, AnalysisPolishResult)
    except SkillExecutionError as e:
        logger.warning("analysis 润色失败，保留原文: %s", e)
        out.analysis_polish = "failed"
        return
    text = res.polished_analysis_text.strip()
    if not text or any(h not in text for h in SECTION_HEADINGS):
        logger.warning("润色输出为空或缺失段标题，保留原文")
        out.analysis_polish = "failed"
        return
    # 原子性：校验全部通过后一次性赋值；summary 单独为空时保留原值
    out.analysis_text = text
    if res.polished_lead_summary.strip():
        out.lead_summary = res.polished_lead_summary.strip()
    if res.polished_profile_summary.strip():
        out.profile_summary = res.polished_profile_summary.strip()
    out.analysis_polish = "polished"
```

- [ ] **Step 4: 运行确认通过**

Run: `python -m pytest tests/test_analysis_polish.py -v`
Expected: 全部通过

- [ ] **Step 5: 读回 `app/skills/analysis_polish.py` 检查中文注释无乱码**

- [ ] **Step 6: Commit**

```bash
git add app/skills/analysis_polish.py tests/test_analysis_polish.py
git commit -m "feat(v1.6.4): apply_polish 润色共享模块（fail-open+结构校验）"
```

---

### Task 4: 复核逻辑抽出为共享模块 user_review.py（纯重构 + 日志修复）

**Files:**
- Create: `app/skills/user_review.py`
- Modify: `app/workflow/pipeline.py`（119-201 行区域：删除迁出代码、改调用）
- Modify: `app/api/agent2.py:13`（GRADING_STANDARD 改从 user_review import）
- Modify: `tests/test_user_review.py:13-14`（import 来源更新）

**Interfaces:**
- Consumes: `UserLeadResult` / `UserLeadReviewResult`、`app.skills.executor.PROMPT_DIR / SkillExecutionError`。
- Produces: `app/skills/user_review.py` 暴露 `GRADING_STANDARD: str`、`USER_REVIEW_SKILL = "user_lead_review"`、`CONCLUSION_ANCHOR = "五、总体评价"`、`_revise_analysis(analysis_text, revised_conclusion) -> tuple[str, str]`、`async def apply_review(executor, evidence_json: str, our_models_summary: str, out: UserLeadResult) -> None`（就地修改 out）。Task 5/6 均调用 `apply_review`。

**本任务为行为等价重构**（唯一变化：复核失败从静默 `pass` 变为 warning 日志），不新增用例，以全量测试通过为验收。

- [ ] **Step 1: 新建 `app/skills/user_review.py`**

```python
import json
import logging

from app.schemas.skills import UserLeadResult, UserLeadReviewResult
from app.skills.executor import PROMPT_DIR, SkillExecutionError

logger = logging.getLogger(__name__)

USER_REVIEW_SKILL = "user_lead_review"

GRADING_STANDARD = (PROMPT_DIR / "grading_standard.txt").read_text(
    encoding="utf-8").strip()

# V1.6.3：analysis_text 第五段的定位锚点，须与定级 Prompt
# （user_lead_analysis_v1.6.3.txt）中的第五个段标题逐字一致。
CONCLUSION_ANCHOR = "五、总体评价"


def _revise_analysis(analysis_text: str,
                     revised_conclusion: str | None) -> tuple[str, str]:
    """用复核给出的新结论替换 analysis_text 的第五段"总体评价"。

    返回 (新 analysis_text, analysis_revision)。纯函数，不抛异常。
    前四段是与等级无关的事实陈述，必须逐字保留——把"原样照抄"交给模型
    做不可靠，只有代码切分能兑现这一点。锚点缺失时退化为文末追加，
    宁可效果打折也绝不失败。
    """
    body = (revised_conclusion or "").strip()
    if not body:
        return analysis_text, "none"
    # 模型可能不听话地把段标题也写进正文，剥掉避免重复标题
    if body.startswith(CONCLUSION_ANCHOR):
        body = body[len(CONCLUSION_ANCHOR):].lstrip("：: \t\n")
    text = analysis_text or ""
    # 总体评价是末段：用最后一次出现，避开前文对该标题的引用
    idx = text.rfind(CONCLUSION_ANCHOR)
    if idx >= 0:
        return f"{text[:idx]}{CONCLUSION_ANCHOR}\n{body}", "replaced"
    sep = "\n\n" if text else ""
    return f"{text}{sep}{CONCLUSION_ANCHOR}（复核修订）\n{body}", "appended"


async def apply_review(executor, evidence_json: str, our_models_summary: str,
                       out: UserLeadResult) -> None:
    """V1.6.2 独立复核 + V1.6.3 叙述修订，就地修改 out。

    fail-open：复核调用失败时等级与文本双双保持初步值（原子性）。
    V1.6.4 起两条路径（V0 流水线 / 对外 API）共用本函数。
    """
    review_context = {
        "user_evidence_json": evidence_json,
        "grading_standard": GRADING_STANDARD,
        "our_models_summary": our_models_summary,
        "preliminary_result_json": json.dumps(
            out.model_dump(), ensure_ascii=False),
    }
    try:
        review: UserLeadReviewResult = await executor.run(
            USER_REVIEW_SKILL, review_context, UserLeadReviewResult)
    except SkillExecutionError as e:
        # V1.6.4：原裸 except pass 会让复核静默失效无从察觉，补日志
        logger.warning("复核节点失败，保留初步定级与叙述: %s", e)
        return
    out.pre_review_grade = out.lead_grade
    out.review_action = review.review_action
    out.review_reason = review.review_reason
    if review.review_action != "confirmed":
        # V1.6.3：等级与对外叙述必须同进同退。先全部算完再一次性
        # 赋值，杜绝"等级已改、文本仍在论证旧等级"的中间态。
        new_text, revision = _revise_analysis(
            out.analysis_text, review.revised_conclusion)
        new_summary = (review.revised_lead_summary or "").strip()
        out.analysis_text = new_text
        out.analysis_revision = revision
        if new_summary:
            out.lead_summary = new_summary
        out.lead_grade = review.reviewed_grade  # type: ignore[assignment]
```

- [ ] **Step 2: 改造 `app/workflow/pipeline.py`**

删除以下内容（已迁入 user_review.py）：
- 19 行 `USER_REVIEW_SKILL = "user_lead_review"` 常量定义；
- 119-120 行 `GRADING_STANDARD = ...` 定义；
- 123-149 行 `CONCLUSION_ANCHOR` 常量与 `_revise_analysis` 函数；
- `run_user_analysis` 中 173-201 行整个 `try/except` 复核块。

顶部 import 区新增（`SKILL_VERSIONS` 字典仍引用 `USER_REVIEW_SKILL`，故需 import）：

```python
from app.skills.user_review import (GRADING_STANDARD, USER_REVIEW_SKILL,
                                    apply_review)
```

同时清理不再使用的 import：`app.schemas.skills` import 行删除 `UserLeadReviewResult`；`app.skills.executor` import 行删除 `PROMPT_DIR`（原仅供 GRADING_STANDARD 读文件，已随迁出）。

`run_user_analysis` 的 else 分支改为：

```python
    else:
        out = await executor.run(
            USER_ANALYSIS_SKILL, context, UserLeadResult)
        # V1.6.2 复核 + V1.6.3 叙述修订（fail-open；被过滤用户跳过）
        await apply_review(executor, context["user_evidence_json"],
                           context["our_models_summary"], out)
```

- [ ] **Step 3: 更新 `app/api/agent2.py` 的 import**

13 行 `from app.workflow.pipeline import GRADING_STANDARD, USER_ANALYSIS_SKILL` 改为：

```python
from app.skills.user_review import GRADING_STANDARD
from app.workflow.pipeline import USER_ANALYSIS_SKILL
```

- [ ] **Step 4: 更新 `tests/test_user_review.py` 的 import**

13-14 行改为：

```python
from app.skills.user_review import CONCLUSION_ANCHOR, _revise_analysis
from app.workflow.pipeline import run_user_analysis
```

- [ ] **Step 5: 全量回归**

Run: `python -m pytest tests/ -q`
Expected: 全部通过（重构不改变行为，`test_user_review.py` 7+5 个用例原样通过）

- [ ] **Step 6: 读回 `app/skills/user_review.py` 检查中文无乱码**

- [ ] **Step 7: Commit**

```bash
git add app/skills/user_review.py app/workflow/pipeline.py app/api/agent2.py tests/test_user_review.py
git commit -m "refactor(v1.6.4): 复核逻辑抽出 user_review 共享模块，fail-open 补日志"
```

---

### Task 5: V0 流水线接入润色 + 版本升级

**Files:**
- Modify: `app/workflow/pipeline.py`（`SKILL_VERSIONS`、`run_user_analysis`）
- Modify: `app/skills/configs/user_lead_analysis.yaml:2`（仅 version）
- Modify: `tests/test_user_analysis.py`、`tests/test_user_review.py`、`tests/test_e2e.py`（Mock 队列补润色响应 + 审计断言）
- Modify: `tests/test_agent2.py`（`test_v121_pipeline_skill_version_bumped` 等版本断言）

**Interfaces:**
- Consumes: Task 3 `apply_polish`、Task 4 `apply_review`、Task 3 测试常量 `POLISH_OK_JSON` / `POLISHED`。
- Produces: V0 路径完整节点序列「过滤 → 定级 → 复核 → 润色」；`SKILL_VERSIONS[USER_ANALYSIS_SKILL] == "1.6.4"`（`user_lead_analysis.yaml` version 同步 `"1.6.4"`，维持「YAML version == SKILL_VERSIONS」不变式；prompt 维持 v1.6.3）。

- [ ] **Step 1: 更新既有测试为"失败在前"**

（a）`tests/test_user_analysis.py`：5 个走定级的用例（`creates_lead` / `upsert` / `filters_hallucinated` / `invalid_lead` / `all_evidence_hallucinated`）队列末尾追加润色响应，并给第一个用例加审计断言。文件顶部加 import：

```python
from tests.test_analysis_polish import POLISH_OK_JSON
```

`test_run_user_analysis_creates_lead` 队列与断言改为：

```python
    provider.queue(NOT_FILTERED_JSON, LEAD_JSON.replace("__CID__", str(c1.id)),
                   REVIEW_CONFIRMED_JSON, POLISH_OK_JSON)
```

函数末尾追加：

```python
    from app.models import AnalysisResult
    res = session.query(AnalysisResult).filter_by(
        target_type="user", target_id=str(u1.id)).one().result
    assert res["review_action"] == "confirmed"     # 复核真实走到
    assert res["analysis_polish"] == "polished"    # 润色真实走到
```

其余 4 个用例：每处 `provider.queue(...)` 在 `REVIEW_CONFIRMED_JSON` 之后追加 `POLISH_OK_JSON`（`test_run_user_analysis_upsert` 排了两轮，两处都加，共 8 个响应）。`test_v16_run_user_analysis_filtered_no_lead` 不加响应，末尾追加断言：

```python
    assert res.result["analysis_polish"] == "none"   # 被过滤账号不润色
```

（b）`tests/test_user_review.py`：3 个改级/确认集成用例补润色响应。文件顶部（常量区之后）加辅助函数：

```python
def _polish_echo(text: str) -> str:
    """润色响应：原样回显 analysis_text，summary 留空表示保留原值。

    集成测试的关注点是复核修订本身，回显可让既有断言原样成立。
    """
    return json.dumps({"polished_analysis_text": text,
                       "polished_lead_summary": "",
                       "polished_profile_summary": "",
                       "confidence": 0.9}, ensure_ascii=False)
```

- `test_upgrade_revises_narrative_and_keeps_first_four_sections`：队列改为
  `provider.queue(NOT_FILTERED_JSON, _lead_json(str(c1.id)), REVIEW_UPGRADED_JSON, _polish_echo(FOUR + CONCLUSION_ANCHOR + "\n" + NEW_BODY))`，
  断言追加 `assert res["analysis_polish"] == "polished"`；
- `test_confirmed_leaves_narrative_untouched`：队列追加 `_polish_echo(PRELIM_ANALYSIS)`，断言追加 `assert res["analysis_polish"] == "polished"`；
- `test_downgrade_also_revises_narrative`：队列追加 `_polish_echo(FOUR + CONCLUSION_ANCHOR + "\n" + down_body)`（`down_body` 定义移到 `provider.queue` 之前）；
- `test_review_failure_keeps_grade_and_narrative_together`：不加响应（复核与润色同时失败），断言追加：

```python
    assert res["analysis_polish"] == "failed"    # 润色也失败，原文保留
```

- `test_filtered_user_skips_review_entirely`：断言追加 `assert res["analysis_polish"] == "none"`。

（c）`tests/test_e2e.py`：52 行 `provider.queue(...)` 末尾追加两个响应（先补上 V1.6.2 起一直缺失的复核响应，再加润色）。文件顶部 import 改为：

```python
from tests.test_analysis_polish import POLISH_OK_JSON
from tests.test_user_analysis import LEAD_JSON, REVIEW_CONFIRMED_JSON
```

队列末尾追加 `REVIEW_CONFIRMED_JSON, POLISH_OK_JSON`。

（d）`tests/test_agent2.py`：`test_v121_pipeline_skill_version_bumped`（375 行起）断言 `"1.6.3"` 改 `"1.6.4"`；若该函数同时断言 `load_skill_config(USER_ANALYSIS_SKILL).version == SKILL_VERSIONS[...]` 则该断言保留不动（不变式）。检查 `test_v163_analysis_config_uses_v163`（590 行起）：`prompt_file` / `prompt_version` 断言维持 v1.6.3 不动；如有 `config.version == "1.6.3"` 断言改 `"1.6.4"`。

- [ ] **Step 2: 运行确认失败**

Run: `python -m pytest tests/test_user_analysis.py tests/test_user_review.py tests/test_e2e.py tests/test_agent2.py -q`
Expected: FAIL——润色响应多排（pipeline 尚未调用润色导致响应错位/断言失败）、版本断言失败

- [ ] **Step 3: pipeline 接线 + 版本升级**

（a）`app/workflow/pipeline.py` 顶部 import 新增：

```python
from app.skills.analysis_polish import apply_polish
```

`SKILL_VERSIONS` 中 `USER_ANALYSIS_SKILL: "1.6.3"` 改 `"1.6.4"`（`USER_REVIEW_SKILL` 维持 `"1.6.3"`）。

`run_user_analysis` 的 else 分支在 `apply_review` 之后追加：

```python
        # V1.6.4：复核后统一润色对外叙述（fail-open，只改文本不改级）
        await apply_polish(executor, out)
```

（b）`app/skills/configs/user_lead_analysis.yaml` 2 行 `version: "1.6.3"` 改 `version: "1.6.4"`（`prompt_file` / `prompt_version` 不动）。

- [ ] **Step 4: 运行确认通过**

Run: `python -m pytest tests/ -q`
Expected: 全部通过

- [ ] **Step 5: Commit**

```bash
git add app/workflow/pipeline.py app/skills/configs/user_lead_analysis.yaml tests/test_user_analysis.py tests/test_user_review.py tests/test_e2e.py tests/test_agent2.py
git commit -m "feat(v1.6.4): V0 流水线接入润色节点，user_lead_analysis 升版 1.6.4"
```

> 说明：升版使 V0 路径旧版本定级结果失效并触发重分析，与历次升版行为一致；API 路径无状态不受影响。

---

### Task 6: 对外 API 路径接入复核 + 润色

**Files:**
- Modify: `app/api/agent2.py:102-109`（`run_profile_analysis` 未过滤分支）
- Test: `tests/test_agent2.py`（既有队列补响应 + 新增 3 个用例）

**Interfaces:**
- Consumes: Task 3 `apply_polish` 与常量 `POLISH_OK_JSON` / `POLISHED`、Task 4 `apply_review`、`tests.test_user_analysis.REVIEW_CONFIRMED_JSON`。
- Produces: API 路径节点序列与 V0 对齐；`ProfileResult.analysis` 为润色后文本（契约字段类型语义不变）。

- [ ] **Step 1: 写失败测试**

（a）`tests/test_agent2.py` 顶部 import 追加：

```python
from tests.test_analysis_polish import POLISH_OK_JSON, POLISHED
from tests.test_user_analysis import REVIEW_CONFIRMED_JSON
```

（b）既有走定级的用例队列补响应：先运行
`python -m pytest tests/test_agent2.py -q` 记录当前全绿基线，然后给每个调用
`run_profile_analysis` 且账号会走到定级的用例，在其 lead 响应之后追加
`REVIEW_CONFIRMED_JSON, POLISH_OK_JSON` 两项。已知涉及（执行时以实际代码为准逐一核对）：
`test_profile_with_screenshot`、`test_profile_no_screenshot_lowers_score`、
`test_v12_profile_upgrade_maps_final_grade`、`test_v12_profile_baseline_c_not_upgraded`、
`test_v121_unrelated_model_downgrade_maps_final_grade`、
`test_v121_our_model_upgrade_maps_final_grade`、`test_v13_profile_result_carries_labels`、
`test_v16_unfiltered_account_proceeds_to_grading`。
纯过滤/空历史/error 路径用例（`test_profile_empty_history_no_value`、
`test_v16_filtered_account_returns_c_without_grading`、
`test_v13_profile_error_item_labels_null`）不加。
若某用例断言了 `r["analysis"]` 的具体文本，改断言为 `POLISHED`（润色后对外文本）。

（c）新增 3 个用例（文件末尾）：

```python
async def test_v164_api_review_upgraded_changes_level():
    """API 路径复核上调：A→H 反映到对外 intent_level，analysis 为润色文本。"""
    from tests.test_analysis_polish import ORIG
    lead = json.dumps({
        "lead_grade": "A", "is_valid_lead": True, "lead_summary": "对比中",
        "evidence_comment_ids": ["u9:0"], "confidence": 0.8,
        "profile_tags": [], "profile_summary": "画像",
        "analysis_text": ORIG}, ensure_ascii=False)
    review = json.dumps({
        "review_action": "upgraded", "reviewed_grade": "H",
        "review_reason": "多条评论持续深度关注，初评偏保守",
        "revised_conclusion": "持续深度关注，判定为高价值线索，建议优先跟进。",
        "revised_lead_summary": "建议优先联系。",
        "confidence": 0.9}, ensure_ascii=False)
    executor, gateway = _executor_and_gateway(
        NOT_FILTERED_JSON, lead, review, POLISH_OK_JSON)
    req = ProfileAnalysisRequest(accounts=[{
        "account_uid": "u9", "account_name": "用户",
        "account_homepage_screenshot": "",
        "comment_history": [{"video_title": "对比", "comment_content": "在看",
                             "comment_time": "2026-07-19T14:23:00+08:00",
                             "comment_like_count": 1}]}])
    out = await run_profile_analysis(executor, gateway, req)
    r = out["results"][0]
    assert r["intent_level_code"] == "high"      # 复核上调生效
    assert r["analysis"] == POLISHED             # 对外为润色后文本


async def test_v164_api_filtered_skips_review_and_polish():
    """被过滤账号不进复核与润色：只消费一个过滤响应。"""
    from app.llm.gateway import LLMGateway
    from app.llm.mock import MockProvider
    from app.skills.executor import SkillExecutor
    from tests.test_user_filter import FILTERED_JSON
    provider = MockProvider()
    provider.queue(FILTERED_JSON, "哨兵：不应被消费")
    gateway = LLMGateway(provider)
    req = ProfileAnalysisRequest(accounts=[{
        "account_uid": "u10", "account_name": "用户",
        "account_homepage_screenshot": "",
        "comment_history": [{"video_title": "分享", "comment_content": "提车三个月",
                             "comment_time": "2026-07-19T14:23:00+08:00",
                             "comment_like_count": 1}]}])
    out = await run_profile_analysis(SkillExecutor(gateway), gateway, req)
    r = out["results"][0]
    assert r["has_value"] is False
    assert "error" not in r                       # 未触发兜底分支
    assert provider._responses == ["哨兵：不应被消费"]   # 复核/润色均未调用


async def test_v164_api_review_failure_fail_open():
    """API 路径复核+润色失败：定级结果原样对外，不产生 error。"""
    lead = json.dumps({
        "lead_grade": "A", "is_valid_lead": True, "lead_summary": "对比中",
        "evidence_comment_ids": ["u11:0"], "confidence": 0.8,
        "profile_tags": [], "profile_summary": "画像",
        "analysis_text": "分析原文"}, ensure_ascii=False)
    executor, gateway = _executor_and_gateway(NOT_FILTERED_JSON, lead)
    req = ProfileAnalysisRequest(accounts=[{
        "account_uid": "u11", "account_name": "用户",
        "account_homepage_screenshot": "",
        "comment_history": [{"video_title": "对比", "comment_content": "在看",
                             "comment_time": "2026-07-19T14:23:00+08:00",
                             "comment_like_count": 1}]}])
    out = await run_profile_analysis(executor, gateway, req)
    r = out["results"][0]
    assert r["intent_level_code"] == "medium"     # 初步 A 级保留
    assert r["analysis"] == "分析原文"             # 文本保留
    assert "error" not in r
```

- [ ] **Step 2: 运行确认失败**

Run: `python -m pytest tests/test_agent2.py -q`
Expected: FAIL（agent2 尚未接线，补排的复核/润色响应未被消费，新用例断言失败）

- [ ] **Step 3: agent2 接线**

`app/api/agent2.py` 顶部 import 追加：

```python
from app.skills.analysis_polish import apply_polish
from app.skills.user_review import apply_review
```

（Task 4 已把 `GRADING_STANDARD` 改从 `app.skills.user_review` import。）

`run_profile_analysis` 中 102-109 行改为（证据包提为变量复用，避免重复构建）：

```python
                # V1.6：定级前先过无效用户过滤（fail-open），命中直接定 C
                evidence = _build_evidence(account, vision_text)
                filt = await run_user_filter(executor, evidence)
                if filt.filtered:
                    out = build_filtered_lead_result(filt)
                else:
                    out = await analyze_account(
                        executor, account, vision_text, our_models_summary)
                    # V1.6.4：API 路径接入复核+润色，与 V0 流水线对齐
                    await apply_review(
                        executor, json.dumps(evidence, ensure_ascii=False),
                        our_models_summary, out)
                    await apply_polish(executor, out)
```

- [ ] **Step 4: 运行确认通过**

Run: `python -m pytest tests/test_agent2.py -q`
Expected: 全部通过

- [ ] **Step 5: 全量回归**

Run: `python -m pytest tests/ -q`
Expected: 全部通过

- [ ] **Step 6: Commit**

```bash
git add app/api/agent2.py tests/test_agent2.py
git commit -m "feat(v1.6.4): 对外 API 路径接入复核与润色节点，与 V0 流水线对齐"
```

---

### Task 7: 文档归档与收尾

**Files:**
- Modify: `claude_docs/versions/V1/OVERVIEW.md`
- Verify: `claude_docs/versions/V1/V1.6/v1.6.4-design.md`（已提交）、本计划文件

**Interfaces:** 无代码接口；按 VERSIONING.md 发版检查清单执行。

- [ ] **Step 1: 更新 `claude_docs/versions/V1/OVERVIEW.md`**

（a）头部「最后更新」改为 `2026-08-13（随 V1.6.4 发布）`；

（b）能力快照 Agent2 条目：在「复核改级时一并修订对外叙述（V1.6.3）……锚点缺失退化为文末追加」之后追加一句：

```
复核后统一接 analysis 润色节点 `user_analysis_polish`（V1.6.4，两路径同接）——针对最终定级重写 analysis_text / lead_summary / profile_summary，清除英文字段泄漏、消除叙述与定级矛盾，只改文本不改级，fail-open 保留原文；复核节点同版起接入对外 API 路径（此前仅 V0 流水线有复核）。
```

（c）Skill/Prompt 版本对照表：`user_lead_analysis` 行改 `1.6.4 | v1.6.3`；表末新增行 `user_analysis_polish | 1.6.4 | v1.6.4`；

（d）变更索引表末新增行：

```
| V1.6.4 | 2026-08-13 | analysis 润色节点：定级+复核后独立 LLM 润色三个对外叙述字段（英文泄漏/定级矛盾/可读性），复核节点补接入对外 API 路径，复核 fail-open 补日志，契约不变 | [design](V1.6/v1.6.4-design.md) / [plan](V1.6/v1.6.4-plan.md) |
```

- [ ] **Step 2: 读回 OVERVIEW.md 修改区域检查中文无乱码**

- [ ] **Step 3: 最终全量回归**

Run: `python -m pytest tests/ -q`
Expected: 全部通过；记录用例总数供发版说明

- [ ] **Step 4: Commit**

```bash
git add claude_docs/versions/V1/OVERVIEW.md
git commit -m "docs(v1.6.4): OVERVIEW 版本对照与变更索引更新"
```

- [ ] **Step 5: 发版观察点（写入交付说明，非代码步骤）**

真实 LLM 联调需观察：① `analysis` 是否仍出现英文字段泄漏与定级矛盾；② `analysis_polish` 分布——`failed` 占比偏高说明润色 Prompt 输出格式不稳；③ API 路径每账号 LLM 调用次数从 2 次升至 4 次（识图另计），关注时延与 tokens 成本（`/audit` 页面按 skill 细分可见 `user_analysis_polish` 与 `user_lead_review` 的调用量）。
